# Relocation-name inventory extent correction (2026-09-24)

`targets/game/reverse/reloc_names.csv` is regenerated by the full byte gate. Its notes must
use the producer's `reloc-derived;call-sites=N;identity=...` format, so the
boundary details previously appended to six rows belong here. The corrections
below change inventory metadata only; a larger inventory extent is not an
accepted byte-match claim.

| RVA | Old/new extent | Retail end evidence |
| --- | ---: | --- |
| `0x00155DA0` | 568/576 | `ret 16` at +0x23D..+0x23F; INT3 at +0x240 |
| `0x003A4A40` | 105/112 | tail jump at +0x6B..+0x6F; INT3 at +0x70 |
| `0x003ADDB0` | 217/224 | `ret` at +0xDF; INT3 at +0xE0 |
| `0x003E3190` | 475/478 | `ret 16` at +0x1DB..+0x1DD; INT3 at +0x1DE |
| `0x003E9D80` | 1206/1213 | `ret 8` at +0x4BA..+0x4BC; INT3 at +0x4BD |
| `0x003FA5B0` | 3951/3964 | `ret 12` at +0xF79..+0xF7B; INT3 at +0xF7C |
| `0x00463340` | 976/982 | branch at +0x373 reaches cold tail +0x3CF..+0x3D5; INT3 at +0x3D6 |
| `0x006C7E90` | 1528/1537 | `ret` at +0x600; INT3 at +0x601 |
| `0x006D7EA0` | 1272/1284 | `ret 40` at +0x501..+0x503; INT3 at +0x504 |

All nine ends precede the next Ghidra function start. The existing matched
rows at `0x003A4A40` and `0x003E3190` already use the larger extent. The
`gen_asm` rows at `0x003E9D80` and `0x00463340` still claim only 1206 and 976
bytes respectively; their frozen dumps and ledger rows were not changed or
credited with the uncovered tails.

The stale `0x0014E2B0` relocation-name candidate was removed because the
independently verified `AI::newOverride` row already claims its complete
574-byte body, and the current harvester excludes claimed addresses. Six more
relocation-name rows were removed for the same claimed-address reason; six
open rows had only their free-form notes normalized to the producer format.
Names, sources, and call counts on those open rows were not revised.
