# Gap analyst: 0x0032D643..0x003307E0 (gpt-6-astra with Ghidra, 2026-09-22)


---

# Gap 0032D643 investigation

Started 2026-09-23 00:09:14 UTC. Hard stop 03:09:14 UTC (180 minutes).
No git, full build, full gate, game launch, tool/doc edits or manual ledger changes.

## Boundary inventory

One function at RVA 0032D720. Code 9260 bytes; owned switch table 716 bytes; complete compiler body 9976 bytes.
- 0032D643..0032D720: 221 bytes, all CC alignment padding.
- 0032D720..0032FB4C: fully decoded x86 code (code_disassembly.txt). Final default epilogue RET 4 at 0032FB49.
- 0032FB4C..0032FE18: 179 little-endian VA entries consumed by JMP [EDX*4+0072FB4C] at 0032D73A. Range check accepts condition types 5..183. Every table target is an instruction boundary in this body. Data belongs to this function, not separate callable entries.
- 0032FE18..003307E0: 2504 bytes, all CC linker padding.

Start evidence: ILT E9 at 0000D9E0 directly targets 0032D720. Slot 9 of vtable VA 010E1F44 is VA 0040D9E0. Constructor 00324490 and landed ScriptConditions destructor 003244B0 install the table (vtable_evidence.txt).

Ghidra has no containing function and its decompile helper produced no draft. The ordinary context_pack tool also returns only 'not in ledger'; evidence was recovered from retail bytes instead. The cached E8-only call index misses the E9 entry thunk, so absence there is not absence of callers.

## Identity and layout

The owning class is ScriptConditions, proved by installed vtable and landed destructor. Slot 9, the bool return / ret 4 ABI, Condition layout and ZH dispatcher together identify evaluateCondition. The existing ledger symbol with that name points at 003307E0 and explicitly documents that it is misnamed; this investigation does not silently move that unrelated row.

Proved Condition fields (name_oracle and bfme_layout cross-checked): +04 dword condition type (0032D724), +08 signed dword parameter count (0032D741), +0C and following four-byte slots Parameter pointers (0032D748, 0032D7A1, 0032D795). Inline getParameter bounds checks return null when index >= count. Incoming ECX remains the ScriptConditions receiver across each helper call. No EH frame. No string literals in the dispatcher itself.

Further contracts, probe results and timing will be appended as measured.

## Complete evidence and contract audit

All 4,572 decoded instructions in the 9,260-byte code extent are reachable from the proved entry, using the actual branches and the 179-entry jump table. The table has 172 distinct code targets: 171 handled condition values plus the shared default for eight values. No instruction starts were invented from the table data. `boundary_validation.txt` preserves the constructor/vtable instructions and reachability result.

The entry thunk is a **jump**, not a direct call: the FUNCTIONS.csv callers value 1 counts this proved REL32 incoming edge. Dispatch also arrives indirectly through installed vtable slot 9. No claim is made that the cached direct-call index inventories virtual callers.

| Object or access | Width / offset | Witness | Status |
| --- | --- | --- | --- |
| ScriptConditions primary vptr | pointer at +0 | 00324498 and 003244CD store VA 010E1F44 | proved |
| Dispatcher entry | vtable +24 (slot 9) | VA 010E1F68 contains VA 0040D9E0; E9 targets 0032D720 | proved |
| Condition type | signed enum-sized dword at +04 | 0032D724; subtract 5 then unsigned range check against 178 | proved |
| Condition parameter count | signed dword at +08 | 0032D741 and repeated signed JLE guards | proved |
| Condition parameter array | four-byte pointers +0C, +10, +14, +18, +1C, +20 | 0032D748, 0032D7A1, 0032D795, 0032E965, 0032E959, 0032E9C5 | proved |
| Parameter field used in condition 166 | dword at +08, tested for nonzero | 0032F889 / 0032F8A1 and SETNE DL | proved offset/width; field name kept opaque |
| Tactical-view global | pointer at VA 012F1600 | 0032D95D; existing TheTacticalView pin and landed camera source agree | proved address; global identity supported by independent landed code |
| Camera query slot | vtable +74, no stack arguments, AL result | 0032D965 | proved ABI; isCameraMovementFinished identity supported by ZH dispatcher; opaque view used because ZH virtual order differs |
| Victory-conditions global | pointer at VA 012F079C | 0032E504 / 0032E516; existing TheVictoryConditions pin | proved address; identity supported by independent construction evidence in pin notes |
| Allied-victory / defeat queries | vtable +34 and +38, no stack arguments, AL result | 0032E50C / 0032E51E | proved ABI; meanings agree with ZH dispatcher |
| Own readiness query | vtable +28, three Parameter pointers and flag; 16 outgoing bytes | 0032EE14 / 0032EE27 / 0032EE5A / 0032EE6C | proved call shape; flag meaning from ZH helper signature |
| Dispatcher return | low-byte boolean, incoming stack cleanup 4 | default XOR AL,AL and RET 4 at 0032FB49; ordinary case returns and tail calls | proved ABI |

The source includes the existing `GameLogic/Scripts.h` for Condition and the upstream ScriptConditions header for 50 already supported callees. It does not redeclare those covered types. Parameter's ZH layout disagrees with this retail read: getInt() compiled +0C, whereas retail reads +08. An address-qualified three-dword view models only the witnessed field. No shared header was edited.

`callee_contracts.json` records each encoded thunk, physical destination, existing row and pins. `callee_abi.csv` records the outgoing argument byte count and separately decoded callee returns. A control-flow audit, excluding inline tables, confirms all reachable RET cleanups for 144/148 helpers. `tail_contracts.txt` independently follows three additional external tail transfers: 003222B0 -> 001A3510 (ret 4), 00322260 -> 001A3570 (ret 4), and 00324360 -> 003BCD40 (plain ret). The last helper, 003260E0, has a proved ret-4 failure path and a dynamic virtual tail through view+68 on success; the success signature is supported by the already landed camera source, rather than a complete target-set proof in this session.

For unresolved helpers, the bank's member declarations preserve incoming ECX and the observed stack arguments. **Assumed where the callee ignores incoming ECX:** original member-vs-free source signature, unused receiver type, and bool-vs-char return spelling. The caller consumes AL; an existing ledger's char/int/free-function spelling is not silently changed or used as independent proof of a member identity. Integer flag declarations preserve the observed four-byte stack values but are not claims about original enum names. No new callee pin was installed.

## Reconstruction, probes, and landing result

- Probe 001: ours 9976, retail 9976, 2 differing non-relocation bytes. Both were the Parameter +0C versus +08 read. Score = 1 - 2/9976 = 0.9997995188452286.
- Probe 002: corrected the witnessed Parameter offset. Ours 9976, retail 9976, 0 differing non-relocation bytes. Measured score 1.0.
- Probe 003: reused 50 existing typed ScriptConditions declarations. Still 9976/9976 and 0 differing non-relocation bytes; score 1.0.
- Independently checked all **180 internal DIR32 relocations**, including every one of the 179 switch entries and the table base, against COFF symbol offsets and retail. They all match. These are not accepted merely because probe masks addresses.
- There are 325 external REL32 sites naming 148 helper symbols. Current existing declarations resolve 50 symbols. Three global DIR32 sites use the two proved global addresses.
- Both requested `add_match.py` attempts ran only the scoped verification. Both failed on unresolved external helper symbols and automatically reverted their candidate row. The second/final attempt has **98 unresolved symbols**. **Landed bytes: 0.**

The final dependency inventory is `DEPENDENCIES.csv`:

- 25: existing different owner or free-function ABI requires reconciliation.
- 32: existing BFME method absent from upstream header or differing signature.
- 21: generated placeholder with no typed contract.
- 20: unclaimed physical body.

The biggest obstacle is **callee ABI/name reconciliation and the permitted ledger scope**, not compiler instruction shape. Twenty helper bodies are themselves unclaimed outside this assigned region, and 21 are only generated placeholders. Several old free-function rows disagree with the natural ScriptConditions member contract. This needs broader dependency work, not a speculative single pin. The task permits only add_match/re_log ledger paths and excludes manual symbols.csv edits; manufacturing aliases solely to satisfy the caller was not an acceptable landing.

The separate historical `?evaluateCondition@ScriptConditions@@UAE_NPAVCondition@@@Z` row at 003307E0 remains untouched. Its own notes say it represents `ScriptEngine::initConditionTemplates`; the new bank uses `Rva0032D720::evaluate` to avoid reusing that conflicting identity.

## Bank and handoff

`body_0032d720.cpp` is the exact source measured by probe 003 (byte-for-byte identical, SHA256 below). It is natural C++ with an ordinary switch, bounded parameter access, ordinary member calls, and no inline assembly, naked functions or embedded bytes. It has been banked using re_log as a **partial, score 1.0**, because the score measures byte shape with relocations excluded and the strict linked gate remains red. A 1.0 score is not a landing receipt.

The temporary unlanded production file `Code/GameEngine/Source/GameLogic/ScriptEngine/Rva0032D720Dispatcher.cpp` was removed after preserving the measured source. The bank is also under `reverse/attempts/0x0032d720.cpp`; re_log saved the immutable attempt archive. No unmatched reconstruction remains in Code.

Source SHA256: `a0cc751a403111ba1fdedb0933bbe21d77b9c04ce856270fa7879c6c3a3bb153`.

Bank recorded at 2026-09-23T00:24:06.133012+00:00; elapsed **14.87 minutes for the one function**, including mapping, reconstruction, three probes, two scoped landing attempts, and ABI/table auditing. Documentation and final integrity checks followed within the 180-minute cap.

All 12,701 assigned bytes are accounted for: 9,976 function bytes (including owned table) and 2,725 CC padding bytes. Six byte values inside the body happen to equal CC; hence the assigned count of 9,970 non-CC bytes. There are no unexplained residual ranges and no additional proved functions in this gap.

Final integrity checks completed at 17.05 minutes total (one function). Preferred bank equals probe 003 source; assigned interval remains unclaimed in the ledger.

To reproduce the shape probe from the final artifact: `py -3 tools/probe.py build/gap_0032d643/body_0032d720.cpp "?evaluate@Rva0032D720@@QAE_NPAVCondition@@@Z" 0x0032D720 --size 9976`. This is not the strict linkage gate.


---

## FUNCTIONS.csv

```
rva,size,start_evidence,end_evidence,callers,vtable,class_hint
0x0032D720,9976,"Decoded E9 at RVA 0x0000D9E0 targets 0x0032D720; 221 preceding CC bytes; vtable slot reaches this thunk","ret 4 at 0x0032FB49 ends code at 0x0032FB4C; owned 179-dword jump table ends 0x0032FE18 followed by 2504 CC bytes",1,"VA 0x010E1F44 slot 9 (+0x24) = VA 0x0040D9E0; installed at RVA 0x00324490 and ScriptConditions dtor 0x003244B0",ScriptConditions

```
