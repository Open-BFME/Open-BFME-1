# Phase 4: Resolve selected deeper naming debt

## Objective

Use elapsed 7:00–10:30 to fix the highest-value remaining evidenced identities
and extend the cleanup families whose measured results justify more work.

## Implementation

- Rank remaining work by evidence strength, reader impact and observed gate cost.
  Keep the productive naming and placement lanes active; evidence research has
  been running since phase 2, so uncertain identities should arrive with briefs.
- Investigate W3DDevice/GameClient/W3DDisplayDrawRectClock.cpp's BfmeV1207,
  BfmeA1207, bfmeDo1207 and addRect006e only with matched-caller/helper evidence.
  Trace all mangled references and pins before any type/function rename; keep
  x87-sensitive expressions intact. Bank facts and retain unknown names if proof
  remains insufficient after the bounded investigation.
- Reviewed descriptive file moves for Common/Q4IntScalarHeapSort.cpp and
  Rva00654FC0DestroyAuxDeque.cpp can describe STLport int heap/sort and
  deque<PSRequest> instantiations without inventing original game class names.
  The automatic renamer refuses templates; review these proposals manually.
- Only schedule a shared-header/layout cleanup if its complete dependent scope
  and identity evidence are known and the measured full gate fits the remaining
  reserve. Coordinator owns that edit and pauses conflicting workers. No broad
  deduplication or shared-header refactor for a metric alone.
- Hours 8–10 independently review the riskiest accepted identity, compare the
  earlier experiments and choose the next measured improvement. Close intake
  of risky work at 10:30 or earlier if observed gate times require it.

## Verification

- Identity brief cites retail boundary and named caller or other corroborated
  evidence, addresses known conflicts, and covers all changed mangled uses.
- `python3 tools/pin_consistency.py --symbol '<name>'` before pin changes,
  `python3 tools/pin_consistency.py --check` afterward, plus
  `python3 tools/identity_guard.py`. No baseline is increased.
- `./build.sh <all-affected-sources>` passes; run `./build.sh` in full for every
  common-runbook trigger. Pins and dependent callers must still match after
  integration/rebase. Record explicit evidence for unresolved candidates.
- Published SHA ancestry, staged readability and concrete cleanup counts are
  recorded. The checkpoint contains hours 8–10 and no unowned pending edits.
