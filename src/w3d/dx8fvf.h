#pragma once
// Guard-only stand-in for WW3D2 dx8fvf.h. The vertex format structs it declares are
// only used on the stubbed DX8 render path; VertexFormatXYZDUV1 (needed more widely)
// lives in the dx8wrapper.h shim.
#ifndef DX8FVF_H_SHIM
#define DX8FVF_H_SHIM
#endif
