#pragma once
// Guard-only stand-in for WW3D2 dx8indexbuffer.h. Only the stubbed DX8 render path
// uses index buffers; file-scope pointers need just these forward declarations.
#ifndef DX8INDEXBUFFER_H_SHIM
#define DX8INDEXBUFFER_H_SHIM
class DX8IndexBufferClass;
class SortingIndexBufferClass;
class DynamicIBAccessClass;
#endif
