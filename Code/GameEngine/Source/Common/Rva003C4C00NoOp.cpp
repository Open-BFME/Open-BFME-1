// The generated thunk j_000070ae calls the one-byte ret body at retail 0x003C4C00.
// The body has no caller-derived semantic identity, so the source keeps its address.

void Rva003C4C00NoOp()
{
}

// cl: /DNDEBUG /DWIN32 /D_WINDOWS /MD /EHsc
