// cl: /O2 /MD

// The carved boundary at retail 0x00471230 contains one ret byte.
// No caller or adjacent body proves a semantic owner, so the name keeps the
// retail address without inventing a class or method.
void Rva00471230NoOp()
{
}
