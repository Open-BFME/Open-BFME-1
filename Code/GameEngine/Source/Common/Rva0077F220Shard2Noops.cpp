// Address-qualified carved no-ops selected as fresh shard-2 fallbacks. Each
// has a one-byte RET boundary and only an unresolved ILT thunk caller, so no
// semantic identity is asserted.

void Rva0077F220Noop() {}
void Rva0077F240Noop() {}
