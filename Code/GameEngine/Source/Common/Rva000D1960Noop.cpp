// cl: /DNDEBUG /MD /EHsc

// The carved boundary at 0x000D1960 contains only a one-byte ret.
// No evidence proves a semantic owner, so the source keeps the retail address.
void Rva000D1960Noop(void)
{
}
