// The carved body at 0x0073ABD0 contains only ret.  Its callers do not prove
// a semantic name, so the function keeps its retail address in its name.
// cl: /O2 /Ob1 /DNDEBUG /DWIN32 /D_WINDOWS /MD /EHsc

void Rva0073ABD0Noop()
{
}
