// cl: /DNDEBUG /MD /EHsc /DWIN32

// Retail 0x002584B0 contains only ret at the carved boundary.
// No caller or table proves a semantic owner, so the source keeps its address.

void Rva002584B0Noop(void)
{
}
