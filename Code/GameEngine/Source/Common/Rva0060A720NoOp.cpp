// cl: /DNDEBUG /MD /EHsc

// The carved boundary at 0x0060A720 contains only ret. No caller proves a
// semantic owner or function name, so the source keeps the address-derived
// identity.
void Rva0060A720()
{
}
