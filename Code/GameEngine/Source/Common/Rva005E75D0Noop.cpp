// cl: /O2 /DNDEBUG /MD

// The carved boundary at 0x005E75D0 contains only a retail ret instruction.
// No evidence identifies a semantic owner, so the name keeps the address.
void Rva005E75D0Noop()
{
}

// The carved boundary at 0x005E75E0 contains only a retail ret instruction.
// No evidence identifies a semantic owner, so the name keeps the address.
void Rva005E75E0Noop()
{
}
