// cl: /DNDEBUG /MD /O2 /EHsc
// Retail 0x00537570 is a one-byte carved body with no proven semantic name.
// Keep its address in the symbol so the source makes no identity claim.

void d_00537570(void)
{
}
