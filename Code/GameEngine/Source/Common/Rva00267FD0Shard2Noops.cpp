// Address-qualified carved no-ops from the assigned and fresh shard-2 queues.
// Each has a one-byte RET boundary and only an unresolved ILT thunk caller, so
// no semantic identity is asserted.

void Rva00267FD0Noop() {}
void Rva0044CC20Noop() {}
void Rva0044CC90Noop() {}
