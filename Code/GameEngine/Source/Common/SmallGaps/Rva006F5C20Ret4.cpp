// cl: /DNDEBUG /MD

// The carved 3-byte body proves only a __thiscall method with one 4-byte
// argument and no work. Its address-derived name keeps the unknown owner out
// of the ledger until a caller or table identifies it.
class Rva006F5C20
{
public:
	void invoke( int unused );
};

// ?invoke@Rva006F5C20@@QAEXH@Z
void Rva006F5C20::invoke( int unused )
{
	(void)unused;
}
