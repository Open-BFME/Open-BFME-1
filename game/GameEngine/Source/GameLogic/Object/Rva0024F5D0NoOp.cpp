// cl: /O2 /DNDEBUG /DWIN32 /D_WINDOWS /MD

// The carved boundary contains only ret. The callers and neighboring bodies
// do not prove a semantic owner, so this body keeps the retail address in its name.
__declspec(noinline) void Rva0024F5D0NoOp()
{
}
