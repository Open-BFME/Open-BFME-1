// cl: /DNDEBUG /MD
// Retail RVA 0x002DA300 is an empty one-byte body.
// No caller or type evidence proves a semantic owner, so the name stays address-derived.

class Rva002DA300Empty
{
public:
    void empty();
};

void Rva002DA300Empty::empty()
{
}
