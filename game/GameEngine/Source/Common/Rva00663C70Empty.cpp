// cl: /DNDEBUG /DWIN32 /D_WINDOWS /MD /EHsc
// Retail 0x00663C70 is a one-byte empty body. The carved boundary proves the
// extent, but no caller or owner proves a semantic function name.

__declspec(noinline) void Rva00663C70()
{
}
