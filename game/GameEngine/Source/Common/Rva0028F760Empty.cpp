// cl: /DNDEBUG /MD
// Retail RVA 0x0028F760 is an empty one-byte body.
// No caller or type evidence proves a semantic owner, so the name stays address-derived.

class Rva0028F760Empty
{
public:
    void empty();
};

void Rva0028F760Empty::empty()
{
}
