// cl: /DNDEBUG /MD
// Retail RVA 0x0028F270 is an empty one-byte body.
// No caller or type evidence proves a semantic owner, so the name stays address-derived.

class Rva0028F270Empty
{
public:
    void empty();
};

void Rva0028F270Empty::empty()
{
}
