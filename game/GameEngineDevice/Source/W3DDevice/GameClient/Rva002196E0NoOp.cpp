// cl: /O2 /MD

// The carved boundary at 0x002196E0 contains one ret byte. Its caller and
// neighbouring W3D bodies do not prove a semantic owner, so the name keeps
// the retail address without inventing a class.
void Rva002196E0NoOp()
{
}
