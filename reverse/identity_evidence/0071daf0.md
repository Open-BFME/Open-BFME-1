# W3DShrubBuffer texture collections, RVA 0x0071DAF0

The 838-byte dump ends at the `ret` at +0x345. Its two strings say "Combined shrub texture" and "Combined shrub shadow texture" (VA 0x01120EE8 and 0x01120E30). Its +0x1450/+0x1454 handles, +0x1458/+0x1480 embedded 0x28-byte collections and +0x1E1CD4 array of 0x5C-byte records match the independently landed `W3DShrubBuffer` constructor at 0x00720820. That constructor installs the named class vtable; see 00720820.md. The BFME method spelling is unknown: use `W3DShrubBuffer::rva0071DAF0`.

The four texture loads target 0x0090E910, the existing `BFMEGetWaterTrackTexture` sret function. Each temporary's first pointer is released through the independently matched 0x009EB7A0. The assignment increments a 16-bit refcount at resource+4, releases the old handle and stores the new one, as in the existing W3DWaterTracks handle definition.

Two address-derived ABI adapter pins retain the existing helper ledger identities. The byte resolver does not consume COFF linker `/alternatename` directives, so explicit pins are needed:

* 0x0094D250 is still the dump `d_0094d250`. Its entry copies ECX to EBX, reads the sole stack argument as a handle address, tests its first pointer, searches the collection at this+8, inserts a retained handle/value pair, sets this+0x1C, and ends in `ret 4`. Its input is a const reference to the same texture handle. The independent tree caller 0x007348A0 passes returned texture temporaries in precisely the same way, then releases them. `rva0094D250` asserts only the proven address and ABI.
* 0x0094D9F0 is the already matched `Rva0094D9F0::assign`. Its existing source fills a caller-supplied result handle from this+0x18, increments its resource count, and returns the same pointer. Both shrub and independent tree callers reserve a temporary result slot, pass its address, consume EAX as its address and destroy it at full-expression end. The adapter returning `BFMEWaterTrackTextureHandle` expresses this exact MSVC hidden-result-pointer ABI, while the existing ledger identity is retained.

The callee inventory was read before reconstruction. `name_oracle` has no W3DShrubBuffer or Rva0094D1E0List witness; new uncertain fields and types retain address-derived names. Probe reports 838/838 exact modulo relocation slots; the scoped byte gate checks all relocated operands.
