# `Radar::loadPostProcess` at 0x00107270

* **Owner and slot.** Radar's vftable 0x010888AC has `??_GRadar` (0x00108110)
  in slot 0, the matched `Radar::xfer` in slot 3 and the matched
  `Radar::refreshTerrain` in slot 4. 0x00107270 is slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState, BuildListInfo,
  DockUpdate and Drawable are each held in slot 1 of their class's table.
* **Body.** `refreshTerrain(TheTerrainLogic)` through slot 4 — Zero Hour
  Radar.cpp's `Radar::loadPostProcess` exactly.
* The previous row named it `VirtualSlot4GlobalCallThunk::invokeTerrainLogic`,
  a descriptive name for the call shape with no owner.
