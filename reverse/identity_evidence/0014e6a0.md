# `AIDockMachine::loadPostProcess` at 0x0014E6A0

* **Owner and slot.** The only vftable holding 0x0014E6A0 (directly or through its
  ILT thunk) is 0x01095a38, whose slot 0 is `??_GAIDockMachine`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState (154 B),
  Drawable, DockUpdate, BuildListInfo and Radar (which calls its slot-4
  refreshTerrain) are each held in slot 1 of their class's table.
* **Body.** A lone `ret`. Zero Hour declares AIDockMachine::loadPostProcess protected; its
  body there is nonempty (a call to an empty base loadPostProcess).
* The previous row, `?Rva0014E6A0@@YAXXZ`, carried no identity.
