# Shrub append at RVA 0x00720D10

The 1312-byte body ends at 0x00721230 with `ret 0x2c`. Its own `W3DShrubBuffer.cpp` file literal, call to the matched `W3DShrubBuffer::addTreeType` at 0x0071EB50, and terrain receiver at +0x3098 identify the owner independently. The constructor/vtable/terrain evidence is recorded in [00720820.md](00720820.md). The BFME public method spelling is unproven, so `rva00720D10` retains its address.

The ABI is thiscall with id, by-value 12-byte Coord3D, scale, matrix pointer, random-scale amount, draw-data pointer, shadow integer, and two AsciiString references (eleven stack dwords). Retail's xfer caller at 0x00721380 independently supplies those arguments through ILT 0x00017C6A. Direct callees are the named partition lookup, addTreeType, rva007209F0, StringBase<char>::compareNoCase, and the two game-client random helpers; this conversion adds no pins.

Making the already matched `getPartitionBucket` definition visible before the append body lets MSVC prove that the position reference is not retained or changed. This restores the retail X/Z/Y record copy. Both functions byte-match in the new translation unit (1312 and 231 bytes). The partition lookup keeps its existing symbol, RVA, boundary, and identity. Its original BaseType.h two-instruction float-to-integer helper reproduces the existing x87 floor conversions; a normal C++ cast does not.

## Exact-source name-guard correction

Only `getPartitionBucket` moved from `W3DShrubBuffer.cpp` to `W3DShrubBufferRva00720D10.cpp`. The ledger's symbol at 0x0071BFC0 is unchanged. The former source still contains the same `doLighting`, `cull`, and `pushAsideTree` bodies, and its three remaining claims byte-match after the extraction.

The name guard compares the entire former translation unit with the entire new translation unit because they share the moved ledger address. It consequently pairs `Vector3` with the append routine's previously banked `Rva00720D10Data`, and `doLighting` with the previously banked `rva00720D10`. Neither is a rename: Vector3 remains the included vector type in both files; Rva00720D10Data remains the independent draw-data parameter view; doLighting remains unchanged in its original source. The two correction entries apply only to these exact before/after source hashes.
