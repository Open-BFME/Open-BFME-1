# 0x0029B120 PhysicsBehavior layout and remaining shape

The served 137-byte row is a complete thiscall body ending in `ret 4`. Its direct callers are still generated, so its original method name is unknown. The owner is now supported independently by the matched `PhysicsBehavior` constructor at 0x0029A880 (`Code/GameEngine/Source/GameLogic/Object/Update/PhysicsBehaviorConstructor.cpp`) and destructor at 0x0029AA30 (`PhysicsBehaviorDestructors.cpp`): both establish the compact 0x60-byte class, module data at `this+0x04`, object at `this+0x08`, and a 12-byte `Coord3D` vector at `this+0x20`. Retail 0x0029B120 reads all those fields, stores the vector end at `+0x24`, zeroes two scalars at `+0x50/+0x54`, and tests its flag at `+0x5F`. The inherited `UpdateModule::setWakeFrame` call and adjacent matched `PhysicsBehavior::applyZeroMotiveForce` at 0x0029B310 corroborate the class. The method therefore stays `PhysicsBehavior::rva0029B120`; neither caller nor vtable proves a descriptive method name.

The first retail call is ILT 0x00014D7B -> 0x000B6BB0, the matched 12-byte `Coord3D` copy helper. It receives the vector end twice, begin once, the address of the incoming Boolean argument, and zero, then the return updates vector end. Other direct routes are ILT 0x000157DA -> matched `UpdateModule::setWakeFrame`, ILT 0x0002191D -> matched `Object::notifyModelConditionChanged`, and ILT 0x00014506 -> matched `Object::kill`. The object bit dword at `+0x11C` is inside the 40-byte model-condition flags proved by `ObjectModelConditions.cpp`; the retail mask is 0x80000. This is global bit 115 (word 3 bit 19), which the shipped model-condition name table at VA 0x012A6918 calls `SPLATTED`. That label comes from the shipped table, not the mask by itself.

The first banked typed C++ body probed 137/137 with four relocation operands and 18 non-relocation differences, all in the mask update after +0x61. The constructor/destructor layout and explicit begin/end locals fixed the earlier entry-register drift. Retail loads flags into ECX, loads 0x80000 into EAX, tests EAX against ECX, then ORs and stores ECX before notifying. Raw-word expressions copied flags to EAX, ORed an immediate and compared the result to ECX; register hints, barriers, volatile aliases and direct inlined bit methods did not help. The later exact 0x0029A7D0 and 0x0029A6A0 siblings supplied a changed source-shape lever: a native `_STL::bitset<320>` member through inline `test`/`set` accessors and a separate conditional-update helper. Applying that same proven layer here yielded a 137/137 exact masked probe with all four retail relocation operands. The strict `add_match` gate remains the final byte/route proof.
