# ScriptEngine::getQualifiedTriggerAreaByName at 0x0034CF60

The Open-BFME5 lift put this 526-byte body under
`?doCreateObject@ScriptActions@@IAEXABVAsciiString@@00PAUCoord3D@@M@Z`. The
body is not doCreateObject. It is Zero Hour's
`PolygonTrigger *ScriptEngine::getQualifiedTriggerAreaByName(AsciiString name)`.

Evidence, all read from the retail image:

- **Vtable slot.** ScriptEngine's vtable at VA 0x010E7A30 slot 22 (+0x58, VA
  0x010E7A88) holds VA 0x004352F6, which disassembles to `jmp 0x0074CF60`
  (RVA 0x0034CF60) -- this exact function. Slot 19 (+0x4C) holds VA
  0x00406E10 -> `jmp 0x0073DA70` (RVA 0x0033DA70, getCurrentPlayer). Slot 20
  (+0x50) holds VA 0x00446E9D -> `jmp 0x0074CE80` (RVA 0x0034CE80,
  getObjectTypes). Slot 21 (+0x54) holds VA 0x0042AD0B -> `jmp 0x0074E370`
  (RVA 0x0034E370). ZH's ScriptEngine.h declares getObjectTypes,
  doObjectTypeListMaintenance and getQualifiedTriggerAreaByName in that order
  right after getCurrentPlayer, so slot 22 lands exactly where ZH places this
  method. All four VA->RVA resolutions were re-derived independently from the
  retail EXE (`tools/build.py` section table + capstone disassembly), not
  taken on the converter's word.
- **Callers.** The already-landed, matched ledger rows
  `evaluateSkirmishNamedAreaExists` (0x00325680) and `evaluateNamedExitedArea`
  (0x00324DE0) carry notes recorded at their own landing time --
  "getQualifiedTriggerAreaByName vtable+0x58" -- independently corroborating
  that vtable+0x58 on ScriptEngine is this method, from bodies that were
  verified before this one existed.
- **Callees.** `tools/callees.py 0x0034CF60 526` resolves all 8 distinct call
  targets in the body to existing ledger names (StringBase<char> ctors/concat/
  format, AsciiString::compare via ILT, ScriptEngine::AppendDebugMessage via
  ILT, and the ledger's `Rva000C9420::call` via ILT 0x00015438); 0 unpinned
  targets.
- **Literals.** The body pushes "[Skirmish]MyInnerPerimeter",
  "[Skirmish]MyOuterPerimeter", "[Skirmish]EnemyInnerPerimeter",
  "[Skirmish]EnemyOuterPerimeter", "InnerPerimeter", "OuterPerimeter", "%s%d",
  "!!!WARNING!!! Trigger area '" and "' not found.". These are ZH's
  ScriptEngine.cpp body's strings, in the same order.
- **ABI.** `ret 4` means one by-value AsciiString argument. The return value
  is the TheTerrainLogic slot-36 (`getTriggerAreaByName`) result, or NULL.

The ZH body ported line for line probes EXACT at 526/526
(`tools/probe.py` against the destination source, 0 non-reloc diffs). Strict
REL32 resolution through `build.compile_function` leaves no symbol
unresolved.
