# `MeshGeometryClass::get_shade_indices` at 0x00924600

* **Caller.** Called by MeshGeometryClass::read_vertex_shade_indices (0x009255B0), whose Zero Hour body calls get_shade_indices(true), and by Compute_Vertex_Normals.
* **Body.** It lazily creates the shade-index array and returns its data pointer.
* The C++ in `Code/Libraries/Source/WWVegas/WW3D2/meshgeometry.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
