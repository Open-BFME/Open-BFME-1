# 0x00538700: Rva006709F0::setPlayerStatus (was ?setPlayerName@EstablishConnectionsMenu@@)

The Open-BFME5 lift named this 373-byte body `EstablishConnectionsMenu::setPlayerName`. Both the class and the method are wrong.

## Method: setPlayerStatus, not setPlayerName

- The body takes `(int slot, NATConnectionState state)` (`ret 8`, `this` unused), compares `state` against 2, 3, 4, 5, 6, 1 and 0, and fetches `GUI:WaitingForManglerResponse`, `GUI:WaitingForMangledPort`, `GUI:WaitingForResponse`, `GUI:ConnectionDone`, `GUI:ConnectionFailed`, `GUI:WaitingToBeginConnection` and `GUI:UnknownConnectionState` (VA 0x010FD86C..0x010FD7B8). That is exactly the label set of Zero Hour `EstablishConnectionsMenu::setPlayerStatus` (reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameClient/GUI/EstablishConnectionsMenu/EstablishConnectionsMenu.cpp). BFME adds a `NATCONNECTIONSTATE_NOSTATE` branch that assigns `L" "` (VA 0x01084C34) and writes through `setConnectingPlayerStatus` 0x00538490 instead of a window lookup.
- Its only reference is ILT 0x000012A3, and that ILT's only reference is slot 5 (vtable+0x14) of VA 0x0111A394. `NAT::setConnectionState` 0x00670DC0 loads `TheEstablishConnectionsMenu` (0x012F363C) and calls `[vtbl+0x14]` with `(slot index, state)`. In the base vtable VA 0x010FD79C, slot 5 is ILT 0x00004511 -> the matched `EstablishConnectionsMenu::setPlayerStatus` 0x004B2DC0 and slot 4 is ILT 0x0000B3FC -> `setPlayerName` 0x004B2D10.

## Class: the derived menu Rva006709F0, not the base

- VA 0x0111A394 is installed by constructor 0x006709F0 (ledger `??0Rva006709F0@@QAE@XZ`). That constructor first calls the base constructor 0x004B2C80 through ILT 0x0000F529, and the base constructor installs VA 0x010FD79C. So 0x00538700 is the derived class's override of slot 5. The base's own `setPlayerStatus` is the separate body at 0x004B2DC0.
- The derived class has no proven semantic name, so it keeps the address-derived name `Rva006709F0` that its constructor already carries (NAT_establishConnectionPaths.cpp uses the same spelling).
- Known pre-existing conflict, not resolved here: symbols.csv pins `??_7EstablishConnectionsMenu@@6B@` at 0x00D1A394 and the ledger names that vtable's slot-0 wrapper 0x00670E90 `??_GEstablishConnectionsMenu@@MAEPAXI@Z`, while the matched `EstablishConnectionsMenu::setPlayerName/setPlayerStatus` rows (0x004B2D10/0x004B2DC0) are slots of the base vtable 0x00CFD79C. One of those two class attributions is wrong; this row takes the opaque derived name so it asserts neither.

Byte proof: the clean C++ body in Rva006709F0SetPlayerStatus.cpp probes EXACT (373/373, relocations masked).
