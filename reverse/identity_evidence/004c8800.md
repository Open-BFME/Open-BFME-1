# `LANGameInfo::getPlayerName` at 0x004C8800

* **Body.** `this + 0x58 + who * 0x68` is passed as `this` to ILT 0x0003A20B
  (`GameSlot::getName`, jmp 0x003879C0) with the hidden result pointer, which is
  returned with `ret 8`. The zeroed local is MSVC's by-value return flag.
* **Layout.** The LANGameInfo constructor 0x0068E980 builds 8 slots of 0x68
  bytes at +0x58 (eh vector constructor iterator), and getPlayerLastHeard
  0x00684C60 indexes the same array.
* **Callers.** The game info window refresh 0x004C8B40 calls it with 0 for the
  game name, exactly as Zero Hour's GameInfoWindow.cpp does with
  `((LANGameInfo *)gameInfo)->getPlayerName(0)`; LANAPI::update 0x006870F0 and
  LANDisplayGameList 0x0068EBF0 call it as Zero Hour's LANAPI.cpp and
  LANGameInfo.cpp do.
* **Zero Hour.** LANGameInfo.h declares
  `inline UnicodeString getPlayerName(int who) { return m_LANSlot[who].getName(); }`,
  which compiles to these 38 bytes.
* The previous row, `?bfmeGoDQG@BfmeThingDQG@@QAEPAVBfmeOtherDQG@@PAV2@H@Z`,
  modelled the hidden result pointer as an argument and carried no identity.
