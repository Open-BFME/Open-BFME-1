# `AIPlayer::loadPostProcess` at 0x00160E30

* **Owner and slot.** The only vftable holding 0x00160E30 (directly or through its
  ILT thunk) is 0x010968b0, whose slot 0 is `??_GAIPlayer`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState (154 B),
  Drawable, DockUpdate, BuildListInfo and Radar (which calls its slot-4
  refreshTerrain) are each held in slot 1 of their class's table.
* **Body.** A lone `ret`. Zero Hour declares AIPlayer::loadPostProcess protected; its
  body there is empty.
* The previous row, `?Rva00160E30Noop@@YAXXZ`, carried no identity.
