# `MeshModelClass::read_texcoords` at 0x0096E330

* **Caller.** Called by MeshModelClass::read_chunks (0x0096FF70), whose Zero Hour body calls read_texcoords(cload,context).
* **Body.** The meshmdlio.cpp source is Zero Hour's read_texcoords with BFME's differences.
* The C++ in `Code/Libraries/Source/WWVegas/WW3D2/meshmdlio.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
