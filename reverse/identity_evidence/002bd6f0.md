# `GiantBirdGuardAttackAggressorState::onEnter` at 0x002BD6F0

* **Owner and slot.** The only vftable in the image that holds 0x002BD6F0 (as
  its ILT thunk 0x0001A190) is 0x010C7D88, whose slot 0 is
  `??_GGiantBirdGuardAttackAggressorState` (0x002C1D40, matched). It sits in
  slot 4, State::onEnter (State's slots: onEnter 4, onExit 5, update 6).
* **Body.** It is `AIGuardAttackAggressorState::onEnter` (0x0015CB70, matched,
  same slot of that class's table) instruction for instruction, except that
  the give-up frame is `MAX(8.0f, chase frames) + frame` in float: the 8.0f
  constant is at 0x010C7448.
* Two earlier verdicts called it `AIGuardRetaliateOuterState::onEnter`; no
  table or row supports that name.
