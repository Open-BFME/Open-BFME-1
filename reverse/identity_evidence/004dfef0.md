# SaveLoadMenuSystem at 0x004DFEF0

The retail FunctionLexicon entry at RVA `0x00EA9528` contains the string pointer
`0x0108769C` and callback pointer `0x0043664C`. The string at RVA `0x00C8769C`
is `SaveLoadMenuSystem`. The callback pointer reaches the five-byte ILT thunk
at RVA `0x0003664C`; its bytes are `E9 9F 98 4A 00`, a relative jump to
`0x004DFEF0`. This directly pairs the retail name with the converted body.

The callback's `WindowMsgHandledType(GameWindow *, UnsignedInt, WindowMsgData,
WindowMsgData)` signature matches the existing BFME source declaration and the
retail body compiled from it. `PopupSaveLoad.cpp` also owns the independently
matched `SaveLoadMenuInput`, initialization, update, and save/load helpers used
by this callback. The Zero Hour `SaveLoadMenuSystem` body is a close twin, but
the retail table and thunk supply the identity proof.

The retail call at `0x004DFEF0` reaches the already matched `bfmeReady(int)`
body at `0x004DF100`. Placing that same definition in this translation unit
lets MSVC retain the selected save-info pointer in EDX across the call, matching
all 1,517 callback bytes while keeping the 38-byte helper exact. The helper's
source row was rehomed; it remains one body at one address.

`GameState::saveGame` at `0x00110170` is independently matched with its BFME
five-argument signature. The callback calls it through the retail ILT thunk at
`0x00020CD4`; the canonical symbol's `route=0x00110170` pin passed route and
consistency checks. A translation-unit-only declaration adjustment keeps the
vendored Zero Hour header intact.

The helper move links two still-existing source files in `name_regression.py`.
Its whole-file comparison pairs `BfmeThingDO` in `Bfme5FortySix.cpp` with the
new, unrelated `Rva0111C0C0AudioSlotView` in `PopupSaveLoad.cpp`, and pairs
`m_bfmeHead` with unrelated `m_pad`. Neither is a rename: `BfmeThingDO` and its
member remain in the original file, and the address-qualified audio view is
new for this callback. The two exact-hash correction entries document this
file-pair false positive without weakening the name guard's other checks.
