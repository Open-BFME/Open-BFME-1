# DataChunkOutput::~DataChunkOutput at 0x001036C0

The ledger carried this 297-byte SEH body as `?rewind@FileInputStream@@`, an
Open-BFME5 `__emit` lift (object symbol `_bfme_FileInputStreamRewind_1036C0`).
That name cannot be right: in the Zero Hour twin
(reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/Common/System/DataChunk.cpp)
the whole FileInputStream class, rewind included, sits inside a `/* ... */` block,
and ZH's rewind is a three-line null-checked seek with no EH frame.

The body is ZH `DataChunkOutput::~DataChunkOutput` instruction for instruction:

- +0x38 calls ILT 0x00036282 -> 0x00102E60, the matched
  `DataChunkTableOfContents::write(OutputStream &)`, with ecx = this+8
  (m_contents) and the argument *(this+0) (m_pOut). The write row's notes and
  the DataChunkTableOfContents.cpp header comment already name this call site.
- then fclose(this+4), `TheGlobalData->getPath_UserData()` via ILT 0x000106EA,
  `concat("_tmpChunk.dat", 13)` with the literal at VA 0x01086334 -- the same
  literal the matched constructor `??0DataChunkOutput@@QAE@PAVOutputStream@@@Z`
  at 0x00102BA0 opens with "wb"; here it is reopened "rb", fseek(0, SEEK_SET),
  and a do-while copying 256-byte fread blocks to m_pOut->write (vtable slot 0),
  then fclose.
- the tail inlines ~DataChunkTableOfContents (walk m_list, virtual deleting
  dtor with 1), and the unwind funclet at 0x00BFCA50 destroys this+8 through ILT
  0x0002ADD3 -> 0x001025F0, next to the matched TOC constructor at 0x001025D0.

No `??1DataChunkOutput@@QAE@XZ` row or pin existed. The ZH body compiled as
clean C++ (no naked code, no emitted bytes) probes exact against all 297 bytes
with every REL32 resolving to retail's target. The range and size are unchanged.
