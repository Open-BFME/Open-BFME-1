# `TeamRelationMap::loadPostProcess` at 0x000F2230

* **Owner and slot.** The only vftable holding 0x000F2230 (directly or through its
  ILT thunk) is 0x01085ee0, whose slot 0 is `??_GTeamRelationMap`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState (154 B),
  Drawable, DockUpdate, BuildListInfo and Radar (which calls its slot-4
  refreshTerrain) are each held in slot 1 of their class's table.
* **Body.** A lone `ret`. Zero Hour declares TeamRelationMap::loadPostProcess protected; its
  body there is empty.
* The previous row, `?Rva000F2230Noop@@YAXXZ`, carried no identity.
