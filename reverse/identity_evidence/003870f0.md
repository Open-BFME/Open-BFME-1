# Rva00367E30Logic::rva003870f0 at 0x003870F0

The 45-byte body at 0x003870F0 was claimed as
`?bfmeGoDRE@BfmeThingDRE@@QAEPAVBfmeOtherDRE@@PAV2@PAX@Z`
(Code/GameEngine/Source/Common/BfmeConv767.cpp). That name is an invented
generator spelling with no caller, vtable, string or Zero Hour evidence, and it
declares the raw hidden-pointer ABI (`BfmeOtherDRE *f(BfmeOtherDRE *, void *)`)
instead of the by-value return the body implements. This file records the
evidence for the address-derived replacement
`?rva003870f0@Rva00367E30Logic@@QAE?AVAsciiString@@ABV2@@Z`.

- **Body ABI.** `push ecx; mov eax,[esp+0xc]` reads the second stack argument
  (the name), forwards it to the store at `this+0x170` through ILT 0x00014943
  (0x003636C0), then `mov esi,[esp+0xc]` takes the first stack argument (the
  hidden return slot), copy-constructs `StringBase<char>` (0x00887B60) into it
  from the helper's result, returns it in eax and ends `ret 8`. The zeroed local
  at `[esp+8]` is MSVC's returned-object-constructed flag. That is `__thiscall
  AsciiString f(const AsciiString &)`.
- **Class.** The matched caller `?applyOwner@Rva003BF540@@QAEXPAVGen003BD7D0Node@@@Z`
  (0x003BF190 +0xDA) calls it through ILT 0x000228DB with
  `ecx = [0x012F0898]`, the global `TheBfmeGameLogic` whose pinned type is
  `Rva00367E30Logic`. It pushes the name temporary then the address of its
  `label` local, and moves its EH state from 1 to 3 across the call: the
  callee constructed that local, the RVO shape of an `AsciiString` return.
- **Name.** No semantic identity is proven, so the method keeps the address
  token (AGENTS.md 3a). The rewritten TU probes EXACT 45/45 under the new name.
