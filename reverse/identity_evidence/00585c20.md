# Identity correction for 0x00585C20

The matched caller names the callee directly. The 90-byte body at 0x00585CF0 is
`BfmeHostYF::bfmeUpdateYF_00585CF0(BfmeHandleYF)` in
`Code/GameEngine/Source/Common/Rva00585CF0HandleUpdate.cpp`. Its source calls
`bfmeNoteYF_00585C20(h)` from within that class method, and the scoped build
verifies the caller body. `python3 tools/callees.py 0x00585CF0 90` resolves
ILT 0x00036D7C to 0x00585C20, confirming that this call reaches the body being
renamed.

The caller's class scope and unqualified member call establish the callee as
`BfmeHostYF::bfmeNoteYF_00585C20(BfmeHandleYF)`. The caller's `BfmeHandleYF`
is a one-pointer value type. The callee's `ret 4` consumes exactly that one
stack argument while its `this` pointer remains in ECX, matching the member
signature. The body is 162 bytes and compiles exactly from the same translation
unit; only its three relocation operands are filled from the retail image.

The previous `Rva00585C20Owner` name was address-derived and came from a
standalone near-match. It is superseded by the matched caller's direct method
name and signature evidence above.
