# `AIGroup::loadPostProcess` at 0x00151CF0

* **Owner and slot.** The only vftable holding 0x00151CF0 (through its ILT thunk)
  is 0x01095f24, whose slot 0 is `??_GAIGroup`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's AIGroup::loadPostProcess is empty, declared
  public, and `Code/GameEngine/Source/GameLogic/AI/AIGroup_loadPostProcess.cpp` already carries that body.
