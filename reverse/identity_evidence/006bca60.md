# `W3DGhostObjectManager::loadPostProcess` at 0x006BCA60

* **Owner and slot.** The only vftable holding 0x006BCA60 (directly or through its
  ILT thunk) is 0x0111cfe0, whose slot 0 is `??_GW3DGhostObjectManager`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState (154 B),
  Drawable, DockUpdate, BuildListInfo and Radar (which calls its slot-4
  refreshTerrain) are each held in slot 1 of their class's table.
* **Body.** A lone `ret`. Zero Hour declares W3DGhostObjectManager::loadPostProcess protected; its
  body there is nonempty (a call to an empty base loadPostProcess).
* The previous row, `?Rva006BCA60Empty@@YAXXZ`, carried no identity.
