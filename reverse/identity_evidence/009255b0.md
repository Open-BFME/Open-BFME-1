# `MeshGeometryClass::read_vertex_shade_indices` at 0x009255B0

* **Caller.** Called by MeshGeometryClass::read_chunks (0x00927950) and MeshModelClass::read_chunks (0x0096FF70); both Zero Hour bodies call read_vertex_shade_indices(cload).
* **Body.** It fills the array from get_shade_indices(true) (0x00924600) with the chunk's data, as in Zero Hour.
* The C++ in `Code/Libraries/Source/WWVegas/WW3D2/meshgeometry.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
