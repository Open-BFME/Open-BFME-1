# `InGameUI::getAllSelectedDrawables` at 0x0043B250

The body is `lea eax,[ecx+0x18]; ret`. It was carved under the opaque name
`?get@Rva0043B250Owner@@QAEPADXZ` ("this plus 0x18, owner unknown").

* **Owner and slot.** `??0InGameUI` (0x0044B800, matched) installs the primary
  vftable 0x00CF5B38 at `+0x34`. Slot 63 (`+0xFC`) of that table is the ILT
  thunk 0x000377C7, which jumps to 0x0043B250.
* **Member.** `InGameUI+0x18` is `m_selectedDrawables`
  (`tools/name_oracle.py --class InGameUI --offset 0x18`: confidence 1.00,
  layout witness). Zero Hour's `getAllSelectedDrawables` is
  `return &m_selectedDrawables;`, a const virtual of `InGameUI`.
* **Callers.** `canSelectedObjectsOverrideSpecialPowerDestination`
  (0x0043ED60, matched over its full 143 bytes) calls `TheInGameUI` through
  `+0xFC` and walks the result as a `DrawableList`, as Zero Hour's body does
  with `getAllSelectedDrawables()`.

InGameUI.cpp's own `getAllSelectedDrawables` compiles to these 4 bytes.
