# 0x0046CA40 callback-map result

The retail body is 208 bytes and ends in `ret 8`. The matched `Rva0046F490DrawBoundsOverlay` caller reaches it through ILT `0x0002E249`, passing `this` in ECX, a hidden eight-byte result pointer, and one pointer argument. The missing-map fallback writes `{1.0f, 0}` to that result; the caller reads the first dword as a float threshold and the second as an integer. That independently establishes the return ABI. The caller obtains `this` from global VA `0x012F19E8`, which differs from `TheWindowManager` at VA `0x012F1B40`, so the new method keeps an RVA-qualified owner even though its map at `this+0x94` matches the callback-map layout used by nearby WindowManager methods.

Retail normalizes the pointer argument through ILT `0x00016B08` to `bfmeSkipLevelPrefix` at `0x0046EE10`, builds and destroys a temporary `BFMERetailAsciiString`, and searches the map through ILT `0x00042A28` to the matched AsciiString hash find at `0x0046A610`. The found node's mapped pointer is at `+0x08`; a null mapped pointer constructs and throws `FunctorNotSet`, whose vtable at VA `0x010766DC` is separately proved by its deleting destructor and matched callback-throw sources. A nonnull pointer receives a virtual slot-1 call with the hidden result pointer and normalized key. These physical routes, rather than the aliases on the ILT, are the callee and ABI claims.

Calling the public `hash_map::find` returns an iterator whose node pointer is `_M_cur`; the compiler inlines that wrapper and emits the private const STLport `_M_find` symbol already pinned to ILT `0x00042A28`. Scratch probes showed 208/208 bytes for the new body and 157/157 bytes for the existing `WindowManager::invokeCallback` after removing the TU-wide `private` macro. The strict scoped gate verified both rows, including the new body's 13 relocation operands and the physical `_M_find` call target. The precise original method name is not evidenced; `Rva0046CA40Owner::lookupThreshold_0046CA40` retains the address token.
