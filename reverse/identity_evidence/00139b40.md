# ThingFactory identity at 0x00139B40

The complete retail boundary is `[0x00139B40, 0x00139C9E)`, 350 bytes. It is
preceded by `int3` padding and ends in `ret 4`, establishing a one-argument
member ABI. The receiver enters in `ECX`, is saved in `EDI`, and supplies the
factory fields at `+0x08`, `+0x0C`, and `+0x10`. The argument at the incoming
`+0x08` stack slot is passed to `StringBase<char>::set`, establishing
`const AsciiString &`.

The exact `ThingFactory::findTemplate` implementation at `0x00137E80` calls
this body through ILT `0x0001056E`. It passes its saved factory receiver in
`ECX`, constructs `AsciiString("Un-namedTemplate")` as the sole argument, and
uses the returned pointer as a `ThingTemplate`. The callee allocates `0x4D4`
bytes, references `DefaultThingTemplate` and `***TESTING`, assigns the incoming
name at `ThingTemplate+0x20`, assigns the factory's incrementing 16-bit ID at
`ThingTemplate+0x478`, and inserts the object into the factory list and map.
Together these facts prove `ThingFactory::newTemplate(const AsciiString &)`,
not the queue's former `FileSystem.h` attribution.

The existing bank compiles to the correct 350-byte length but differs in 193
non-relocation bytes. Its remaining frame/register-allocation mismatch is not
an identity or boundary ambiguity.
