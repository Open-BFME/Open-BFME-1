# `RecorderClass::logGameEnd` at 0x00097280

* **Caller.** Called (through ILT 0x0040800D) by RecorderClass::stopRecording (0x000981C0), whose Zero Hour body calls logGameEnd().
* **Body.** BFME's version of the end-of-game log writer.
* The C++ in `Code/GameEngine/Source/Common/RecorderLogGameEnd.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
