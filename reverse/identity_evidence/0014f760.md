# `AIDockProcessDockState::loadPostProcess` at 0x0014F760

* **Owner and slot.** The only vftable holding 0x0014F760 (through its ILT thunk)
  is 0x01095ea0, whose slot 0 is `??_GAIDockProcessDockState`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's AIDockProcessDockState::loadPostProcess is empty, declared
  protected, and `Code/GameEngine/Source/GameLogic/AI/AIDock.cpp` already carries that body.
