# 0x00943FF0 scene-vector bounds update

Retail `[0x00943FF0, 0x009440B9)` is a complete 201-byte `ret 4` thiscall body. Its one stack argument points to six consecutive floats. The body copies all six into offsets `+0x00..+0x14`, computes the larger of `(+0x0C)-(+0x00)` and `(+0x10)-(+0x04)`, and stores `g_bfmeDefaultBU / larger` at receiver offset `+0x20`. The method spelling remains address-qualified because these operations do not prove a published method name.

The physical aggregate beginning at `0x009446F0` contains two independent routes to this body. At `+0x1D0`, a forwarding entry adds `0x34` to `ECX` and tail-jumps to `0x00943FF0`. A separate entry beginning at `+0x1F0` zeros receiver fields `+0x18/+0x1C`, calls this body at `+0x239`, then calls `BfmeSceneVector::Set_Level` at `+0x242` on the same receiver. The already matched `Set_Level` in `scene.cpp` places its vector and count at `+0x18/+0x1C` and its scale at `+0x20`. Retail `0x00943F70` reads the same vector/count fields. This establishes the `BfmeSceneVector` receiver layout without assigning a semantic name to the new method. The aggregate's ledger label alone is not relied on as method identity.

The three direct calls are `0x00943F70` (`BfmeSceneVector::clear`, `ret 4`), `0x00943CF0` (the matched linked-node `process`, `ret 4`), and `0x0082E5F0` (the matched STLport 8-byte node deallocator). The existing `reverse/symbols.csv` candidates for `clear` and `process` are pin-consistent at those physical addresses; no pin was added. The data operand at `+0x7B` is the existing `g_bfmeDefaultBU` candidate at `0x01075334`.

The clean C++ source uses a one-pointer temporary list. Keeping its traversal cursor as a `register` local reproduces the retail saved-ESI loop after the `process` call. The normal STLport public `deallocate` wrapper compiled to an imported indirect call in this translation unit, whereas retail directly calls the out-of-line `_M_deallocate`; the TU-local declaration records that observed direct call. The final source probe matched 201/201 bytes modulo relocation slots. `add_match.py --replace-rva` then strictly verified all 201 bytes and relocation targets, with all 24 matched rows in `scene.cpp` still passing. The generated dump file was not edited.
