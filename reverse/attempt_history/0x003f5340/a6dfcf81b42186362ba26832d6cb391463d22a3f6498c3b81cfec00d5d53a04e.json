{
  "schema": 1,
  "sha256": "a6dfcf81b42186362ba26832d6cb391463d22a3f6498c3b81cfec00d5d53a04e",
  "symbol": "_Rva003F5340@12",
  "score_kind": "author-estimate",
  "score": 0.4573055,
  "source": "// _Rva003F5340@12\n// partial score=0.4573055 date=2026-09-21\n// _Rva003F5340@12\n// Experimental ring-search reconstruction; identity remains address-derived.\n// Retail 0x003F5340: 527 bytes. This variant: 526 bytes, 286 differing bytes,\n// three misaligned relocation sites. Not byte-match proof.\n// Volatile delta reproduces immediate initialization; guarded do-loops model\n// the retail count loops and both step>=0 guards. Compiler still eliminates\n// repeated guards and rotates local stack slots; do not infer a one-byte-only gap.\ntypedef bool Bool;\ntypedef int Int;\n\nstruct ICoord2D\n{\n\tInt x, y;\n};\n\nclass CRCParameterCheck\n{\n};\n\nextern CRCParameterCheck *TheCRCParameterCheck;\nextern bool Glo012F0239;\nextern void j_0003a17a(void);\nextern void j_00046e6b(void);\n\ntypedef void (__cdecl *BfmeCritterDesyncLog)(void *, const char *, ...);\n\nclass BfmeCellTesterRva003F5340\n{\npublic:\n\tbool test(Int x, Int y);\n};\n\ntypedef bool (BfmeCellTesterRva003F5340::*TestMemberFn)(Int, Int);\n\nextern \"C\" Bool __stdcall Rva003F5340(const ICoord2D *center, Int radius, void *userData)\n{\n\tif (Glo012F0239 && TheCRCParameterCheck)\n\t{\n\t\t((BfmeCritterDesyncLog)j_0003a17a)(\n\t\t\tTheCRCParameterCheck,\n\t\t\t(const char *)0x010EFBC0,\n\t\t\tcenter->x,\n\t\t\tcenter->y,\n\t\t\tradius);\n\t}\n\n\tunion\n\t{\n\t\tvoid (*raw)(void);\n\t\tTestMemberFn member;\n\t} fn;\n\tfn.raw = j_00046e6b;\n\tBfmeCellTesterRva003F5340 *tester = (BfmeCellTesterRva003F5340 *)userData;\n\n\tif ((tester->*fn.member)(center->x, center->y))\n\t\treturn true;\n\n\tInt best = 0;\n\tInt dx = 0, dy = 0;\n\tvolatile Int delta = 1;\n\tInt step;\n\tif (radius > 0)\n\t{\n\t\tstep = 4;\n\t\tdo\n\t\t{\n\t\t\tradius -= step + 2;\n\t\t\tInt count;\n\t\t\tif (step > 0)\n\t\t\t{\n\t\t\t\tcount = delta;\n do {\n\t\t\t\t\tdx++;\n\t\t\t\t\tif (best == 0 || dx * dx + dy * dy < best)\n\t\t\t\t\t{\n\t\t\t\t\t\tif ((tester->*fn.member)(center->x + dx, center->y + dy))\n\t\t\t\t\t\t\tbest = dx * dx + dy * dy;\n\t\t\t\t\t}\n\t\t\t\t} while (--count != 0);\n\t\t\t\tif (step > 0)\n\t\t\t\t{\n\t\t\t\t\tcount = delta;\n do {\n\t\t\t\t\t\tdy++;\n\t\t\t\t\t\tif (best == 0 || dx * dx + dy * dy < best)\n\t\t\t\t\t\t{\n\t\t\t\t\t\t\tif ((tester->*fn.member)(center->x + dx, center->y + dy))\n\t\t\t\t\t\t\t\tbest = dx * dx + dy * dy;\n\t\t\t\t\t\t}\n\t\t\t\t\t} while (--count != 0);\n\t\t\t\t}\n\t\t\t}\n\t\t\tif (step >= 0) {\ncount = delta + 1;\n do {\n\t\t\t\tdx--;\n\t\t\t\tif (best == 0 || dx * dx + dy * dy < best)\n\t\t\t\t{\n\t\t\t\t\tif ((tester->*fn.member)(center->x + dx, center->y + dy))\n\t\t\t\t\t\tbest = dx * dx + dy * dy;\n\t\t\t\t}\n\t\t\t} while (--count != 0);\n\t\t\tif (step >= 0) {\ncount = delta + 1;\n do {\n\t\t\t\tdy--;\n\t\t\t\tif (best == 0 || dx * dx + dy * dy < best)\n\t\t\t\t{\n\t\t\t\t\tif ((tester->*fn.member)(center->x + dx, center->y + dy))\n\t\t\t\t\t\tbest = dx * dx + dy * dy;\n\t\t\t\t}\n\t\t\t} while (--count != 0);\n}\n}\n\n\t\t\tif (best != 0)\n\t\t\t\treturn true;\n\t\t\tdelta += 2;\n\t\t\tstep += 8;\n\t\t} while (radius > 0);\n\t}\n\treturn false;\n}\n"
}