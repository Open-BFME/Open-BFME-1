# Taint parent review

Scope: read-only audit of the parent reconstruction in
`/home/thag/Projects/Open-BFME-1/build/impact-20260921/build/r109-typed-radius.cpp`
against its matching retail disassembly/data. No source, probe, pin, ledger, or
commit changes are in scope. The parent file was found in the sibling parent
worktree; this report is in the current worktree as requested.

Status: completed read-only audit. Retail reference: RVA `0x0026B9D0`, size
`1109` bytes, disassembled from the baseline image; helper RVA `0x0026B950`
was not reimplemented or otherwise touched.

## Verified semantic correspondence

- The filter declaration in parent lines 90--120 is 24-byte mask + 24-byte
  mask + 8-byte base (`vptr`/`m_next`) = 56 bytes. `set(151)` is word index 4,
  bit 23. Both range calls pass that selected mask and `KINDOFMASK_NONE`.
  Retail constructs the two filter temporaries in the two EH paths and stores
  the virtual-base value `0x01083B5C` (retail `+00AB`); the source has the
  virtual base destructor needed for that lifetime path.
- The native-float range ABI at parent lines 122--126 is correct. Retail first
  calls the filter constructor, then multiplies the radius by `0x010B4BCC`
  before the first `bfmeForwardWideC` (`+0069`--`+009C`). The second query
  uses `radius * 0x01075340` (`+0192`--`+01B9`). The source uses full
  `radius` for the per-object terrain clear and the post-scan adapter; it does
  not pass the half-scaled query radius to those calls.
- Both loops use the same-owner exclusion (`==`, then `continue`, parent lines
  262--264 and 282--284). This matches retail's back-edges at `+0124` and
  `+02B8`; neither loop uses the opposite-owner test.
- The first loop's order is preserved: pointer-identity tree insertion,
  terrain call with the full radius, then `destroyObject` (parent lines
  266--269; retail `+0126`--`+0153`). The second loop finds already-seen
  pointers and skips them (parent lines 286--288), otherwise derives the
  `0x38` position and `0x98` status-bit amount and calls the taint manager.
  Retail uses the same `0x80000 -> 0xFF/0` conversion at `+02E3`--`+0322`.
- The post-scan terrain calls have the right ABI/order: `(loc, radiusBits, 1)`
  and `(loc, radius + 0x0107FAA8, 1)` (parent lines 301--306; retail
  `+032C`--`+0356`). The FX branch is correctly null/blocked guarded and
  passes `(loc, 0, 0.0f, 0)` (parent lines 308--310; retail `+035B`--`+037A`).
- The OCL call is correctly guarded and ordered as
  `(saved owner, copied Coord3D, 0, 0)` (parent lines 312--318). Retail's two
  zero pushes precede the copied point and its first argument is the saved
  owner at `+03AB`; it is not `loc.x`.
- The `rva0026B950` call is outside the OCL branch, passes `(loc,
  &data->m_taintObject)`, and uses the Taint receiver; this matches retail's
  saved receiver reload at `+03B6` and call at `+03C1`. The final circle call
  directly uses `loc`, full `radius`, amount zero, and `true` (parent lines
  320--323; retail `+03C6`--`+03D1`).
- The result wrapper and tree lifetime are semantically aligned: `next()` reads
  an 8-byte entry and advances the native cursor, the tree remains alive across
  both scans, and the result/tree cleanup occurs after the final circle. This
  matches retail's result handoff/decrements at `+01BE`--`+0270` and cleanup
  at `+03E7`--`+0438`.

## Finding

I found no additional branch-condition, parameter-order, receiver, or callee
ABI error in the parent source. The remaining `514`-byte difference therefore
is not explained by a semantic mismatch visible in this read-only comparison;
it is in the compiler/EH/STL lifetime/code-shape side of the reconstruction.
No probe, source edit, pin, ledger edit, or commit was made. The separate
103-byte `0x0026B950` helper remains outside this review as requested.

## Naming review for the bank replacement

Three detected substitutions require explicit explanation:

- `BitFlags128` was a four-word/16-byte bank shim. Physical constructor
  000C3DD0 copies two six-word/24-byte masks at offsets8 and20 hex, giving a
  56-byte filter. Caller0026B9D0 selects word4 bit23, outside that old128-bit
  layout. Rva000C3DD0Mask corrects this disproven size claim and leaves the
  original logical bit-count unknown;192 denotes the witnessed storage capacity.
- `Coord3D` is not renamed to `Rva0026B9D0Entry`. The local duplicate Coord3D
  declaration is replaced by the included native declaration; all position
  fields and parameters remain Coord3D. The new entry is an unrelated8-byte
  Object-pointer/word element in the returned iterator's vector. The token
  pairing does not represent a type identity substitution.
- `TerrainLogicP48::adapter` remains named adapter and still handles the
  three-argument post-scan call via ILT00042CD0 to001A64F0. The newly declared
  rva001AC3D0 handles a different, two-argument per-object call via ILT000017CB
  to001AC3D0. It is not a downgrade of the adapter name. The old incomplete
  bank did not distinguish these contracts correctly.

These explanations are bound to exact bank snapshots. No naming detector or
baseline is changed. The bank is partial evidence and earns no coverage credit.
