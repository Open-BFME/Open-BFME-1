# Sides parser correction, RVA 0019BF40 / 1197 bytes

The prior 1034-byte bank omitted behavior. This investigation preserves the existing address-qualified identity and introduces no pins or coverage claims.

Retail evidence, offsets relative to 0019BF40:

- +70 explicitly writes the default true flag for versions below 6.
- +C8..DA increments m_numSides before calling the side initializer.
- +110 allocates 0x8C and invokes BuildListInfo constructor 00194210 with native allocation cleanup; the old raw allocation omitted this lifetime.
- +145..19E reads canonical AsciiString temporaries, sets the two name members and releases them. Target 00887C90 is a shared StringBase<char>::set body; the ledger UnicodeString alias does not make these Unicode strings.
- The side, team and script loops reuse the count local. +3CB explicitly resets it to zero before parser registration.
- Constructor 00352810 receives scripts-array, &count, &file and the information label, not NULL then scripts. Callback 001028D0 receives user data as its receiver and calls virtual slot +4 with file/info.
- Registration base destructor cleans its own m_table/m_parser pair, not the outer SidesList. Base vtable VA 0107C7D0 and derived VA 010E8538 independently delimit virtual destructor and parse slots. Vtable entry addresses are VAs, not RVAs.
- The script replacement loop repeats bounds-checked side access after virtual deletion; a callback can change state, so retaining a cached side pointer is not equivalent.

The closest measured candidate uses the existing WWMath Coord3D declarations and definitions from matched coord3d.cpp. It compiles to 1195 bytes versus 1197, with 351 masked differences and a D8 frame versus CC. This does not prove that this inlined BuildListInfo used the WWMath type rather than the released BaseType.h POD type. The POD alternative documented below compiles to 1179 bytes with 603 differences and the correct CC frame; the first difference is the branch displacement at +99. Binding the template-name receiver first makes no byte change. Do not repeat that lever or treat the two-byte size gap as a two-byte mismatch.

Retail stores BuildListInfo position at +0C..14. A name-oracle record currently reports a conflicting +08 location; the actual constructor and this parser outrank that record. Reconcile it independently before landing; do not rename the class to evade the check.

No new production source, pin, or matched ledger row is part of this evidence update. t=22min model=gpt-6-astra; CLI independent audit model=gpt-5.6-luna Max.

## POD alternative delta

Apply this patch to the banked source to reproduce the1179-byte variant.

```diff
--- native-coord.cpp
+++ pod-coord.cpp
@@ -1,6 +1,6 @@
 // ?parseSidesDataChunk@Rva0019BE80SidesList@@QAE_NAAVDataChunkInput@@PAUDataChunkInfo@@@Z
 // partial score=0.25 date=2026-09-20
-// cl: /DNDEBUG /MD /EHsc /ICode/Libraries/Source/WWVegas/WWLib /ICode/Libraries/Source/WWVegas/WWMath
+// cl: /DNDEBUG /MD /EHsc /ICode/Libraries/Source/WWVegas/WWLib
 // Retail 0x0019BF40: SidesList's DataChunk parse callback -- the BFME variant
 // of ZH SidesList::ParseSidesDataChunk (reference/CnC_Generals_Zero_Hour/
 // GeneralsMD/Code/GameEngine/Source/GameLogic/Map/SidesList.cpp:243-319).
@@ -43,20 +43,7 @@
 
 #include "ascii_string.h"
 inline AsciiString::~AsciiString() { ((StringBase<char> *)this)->releaseBuffer(); }
-#include "coord3d.h"
-inline Coord3D::Coord3D() {}
-inline Coord3D::~Coord3D() {}
-inline Coord3D::Coord3D(const Coord3D &that) { x=that.x; y=that.y; z=that.z; }
-inline Coord3DBase &Coord3DBase::operator=(const Coord3DBase &that)
-{
-    struct Raw { unsigned int x,y,z; };
-    *(Raw *)this=*(const Raw *)&that;
-    return *this;
-}
-inline Coord3D &Coord3D::operator=(const Coord3D &that)
-{
-    Coord3DBase *base=this; *base=that; return *this;
-}
+struct Coord3D { float x, y, z; };
 struct DataChunkInfo
 {
 	AsciiString m_label;			// +0x00
```

## Independent CLI audit

A read-only gpt-5.6-luna Max review confirmed the callback arguments, shared count reset, string types, native allocation and repeated bounds checks. Correcting VA/RVA confusion resolved the callback concern: VA00435AAD is thunk RVA00035AAD ->0035BFB0, and callback001028D0 calls virtual slot+4. Before a future production landing, the derived parser virtual must be bound to0035BFB0 through its independently verified retail vtable; the bank only declares it and adds no bindings. The audit did not resolve the coordinate-copy/frame difference.
