# `Player::loadPostProcess` at 0x000DD960

* **Owner and slot.** The only vftable holding 0x000DD960 is Player's
  0x010840CC (slot 0 `??_GPlayer`), in slot 1, BFME's Snapshot
  loadPostProcess slot (slot 2 returns the snapshot name, slot 3 is xfer;
  about forty classes' slot 1 bodies are named on the same evidence, among
  them Radar's, which is Zero Hour's refreshTerrain(TheTerrainLogic)).
* **Body.** A lone `ret`: Zero Hour's Player::loadPostProcess is empty.
* The name was previously on 0x000D9D30, which no table references (see
  000d9d30.md).
