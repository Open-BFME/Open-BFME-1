# `AIGuardReturnState::update` at 0x0015E4C0

* **Owner and slot.** vftable 0x010962C8 has `??_GAIGuardReturnState`
  (0x0015CFC0, matched) in slot 0 and the matched
  `AIGuardReturnState::onEnter` (0x0015CA00) in slot 4. 0x0015E4C0 sits in
  slot 6, State::update (State's slots: onEnter 4, onExit 5, update 6).
* **Body.** Zero Hour's `AIGuardReturnState::update`: once the frame passes
  the next scan time at this+0x50, it re-arms that time from TheAI's settings
  and returns STATE_FAILURE (-2) if the guard machine at this+0x1C finds an
  inner target; otherwise it tail-jumps to AIInternalMoveToState::update.
* The C++ in AIGuard.cpp has always compiled under this name (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
