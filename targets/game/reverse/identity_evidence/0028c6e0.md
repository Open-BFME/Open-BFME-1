# `DeletionUpdate::setLifetimeRange` at 0x0028C6E0

* **Caller.** Called (through ILT 0x00431B33) by SpecialPowerModule::createViewObject (0x00269130), whose Zero Hour body calls dup->setLifetimeRange(visionDuration, visionDuration).
* **Body.** It takes the min and max frame pair and stores the death frame.
* The C++ in `game/GameEngine/Source/GameLogic/Object/Update/DeletionUpdate.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
