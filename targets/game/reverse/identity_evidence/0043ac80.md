# InGameUI hint slots 27-29: `createMoveHint`, `createAttackHint`, `createForceAttackHint`

Zero Hour's `InGameUI.h` declares, in order, `beginAreaSelectHint`,
`endAreaSelectHint`, `createMoveHint`, `createAttackHint`,
`createForceAttackHint`, `createMouseoverHint`, `createCommandHint`,
`createGarrisonHint`. InGameUI's primary vftable 0x00CF5B38 (installed by
`??0InGameUI`, 0x0044B800) holds them in slots 25-32:

| slot | body | ledger before | what the body is |
|---:|---|---|---|
| 25 | 0x0043AC30 | `beginAreaSelectHint` | |
| 26 | 0x0043AC70 | `endAreaSelectHint` | |
| 27 | 0x00444450 | `d_00444450` (dump) | Zero Hour's `createMoveHint`: `getSelectCount()==1` (`+0xF0`) test on the first selected drawable's object, then stores `getArgument(0)` location and `TheGameClient`'s frame in the 25-entry hint ring at `+0x38` and advances the index at `+0x22C` with wrap at 25 |
| 28 | 0x0043AC80 | `createMoveHint` | `ret 4`: Zero Hour's empty `createAttackHint` |
| 29 | 0x0043AC90 | `createAttackHint` | `ret 4`: Zero Hour's empty `createForceAttackHint` |
| 30 | 0x004445C0 | `createMouseoverHint` | |
| 32 | 0x0043ACA0 | `createGarrisonHint` | calls `getArgument(0)` like slot 27 |

The two empty bodies carried the names one slot early. Slot 27 is the only
body in the run that does what `createMoveHint` does, so the empty ones are
`createAttackHint` and `createForceAttackHint`, in declaration order.
