# `AIGuardReturnState::loadPostProcess` at 0x0015CFA0

* **Owner and slot.** The only vftable holding 0x0015CFA0 (through its ILT thunk)
  is 0x010962c8, whose slot 0 is `??_GAIGuardReturnState`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's AIGuardReturnState::loadPostProcess is empty, declared
  protected, and `game/GameEngine/Source/GameLogic/AI/AIGuard.cpp` already carries that body.
