# ScriptActions::doTeamSetModelConditionForDuration at 0x002FAD50

The 358-byte body at 0x002FAD50 was claimed as
`?doTeamSetModelConditionForDuration@ScriptActions@@IAEXABVAsciiString@@0MH@Z`,
whose fourth parameter was declared `Int percent`, inherited from the UNIT
sibling at 0x002FABE0. This file records why it is a `Real` and the row is
renamed to
`?doTeamSetModelConditionForDuration@ScriptActions@@IAEXABVAsciiString@@0MM@Z`.

- **This body never reads it.** Retail reads only entry+4, +8 and +0xC before
  `ret 0x10`, so the body alone cannot type the fourth argument.
- **Caller supplies a Real.** executeAction (0x00303BF0) case 435 at +0x5CB8
  forwards `Parameter[3]+0xC` as the fourth argument, exactly as case 434
  (UNIT sibling, +0x5C47) does; ScriptEngine init gives templates 434 and 435
  the same parameter-3 type.
- **The sibling proves the type.** The UNIT sibling passes that same argument
  to `?rva001D02C0@Object@@QAEXHHM@Z` (0x001D02C0), whose body
  `fmul`s it at +0x72 (see 002fabe0.md). So the value is a `Real`.
- The type only changes the mangled name; the TU probes EXACT 358/358 under
  the new name.
