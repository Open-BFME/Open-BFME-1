# Rva006B0850XferAsciiStringRealMap at 0x006B0850

The 414-byte body at 0x006B0850 was claimed as `?xferMap@@YAPAVXfer@@PAV1@PAX@Z`.
The only evidence for `xferMap` was the pin `?xferMap@@YAXPAVXfer@@PAX@Z` on
ILT 0x000149D4, a label the caller-side extern in Rva006ABC60Xfer.cpp invented
(commit 924dde8a67, reason "xfer map helper call through the existing STL xfer
thunk"). That pin proves the call edge 0x006B0CB0 -> ILT 0x000149D4 ->
0x006B0850, not the name. This file records the evidence for the
address-derived replacement
`?Rva006B0850XferAsciiStringRealMap@@YAPAVXfer@@PAV1@PAX@Z`.

- **What the body is.** Its only string literals are "std::map" (0x0111C360,
  the xferTypeName argument) and "Map must be empty on load" (0x0111C340); the
  load path indexes an AsciiString-keyed map through its operator[] at
  0x006AF6D0 (ILT 0x0000D9D1) and transfers each value with Xfer::xferReal.
  It returns the Xfer in eax (`mov eax,esi`).
- **Why the address token stays.** targets/game/reverse/string_xrefs.tsv shows a family of
  STL-container xfer helpers ("std::vector"/"Vector must be empty on load",
  "std::list", "std::set", "std::map"), whose source template or overload name
  nobody has recovered. Landed members keep the address token
  (Rva00372670XferObjectIDVector, Rva000D7010XferScienceVector,
  Rva003C2830XferMissionObjectiveStateVector), per AGENTS.md 3a.
- **One identity.** The caller Rva006ABC60::xfer now binds this name through
  ILT 0x000149D4, and the invented ILT pin is dropped.
- The TU probes EXACT 414/414 under the new name.
