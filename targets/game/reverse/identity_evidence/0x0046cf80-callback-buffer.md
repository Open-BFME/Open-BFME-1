# 0x0046CF80 callback buffer lookup

Retail 0x0046CF80 is a 251-byte thiscall body ending in `ret 4`. It allocates a 255-byte character buffer, clears its first byte, and looks up its pointer argument as an `AsciiString` key in the table at `this+0x1C`. On a miss it retries with `bfmeSkipLevelPrefix(name)`. Both lookups call ILT 0x0004B231, which jumps to the matched STLport `AsciiString` hash finder at 0x0046A200. The mapped pair holds a callback pointer at node+0x08 and an opaque value at node+0x0C. A null callback throws the same `FunctorNotSet` exception as the matched callback dispatcher. A nonnull callback receives `(value, buffer, 0)` through virtual slot 1; the body returns the buffer pointer.

The `Rva0046C2A0Mapped` type and member table offset are independently established by matched `WindowManager::bindShownWithArg`, and the nearby matched `WindowManager::invokeCallback` establishes the temporary `BFMERetailAsciiString` and exception shape. The direct caller through ILT 0x0001DF43 is still anonymous, so the prospective method keeps an RVA-qualified owner and `format_0046CF80` address token rather than asserting a descriptive WindowManager API name.

The banked clean source in `targets/game/reverse/attempts/0x0046cf80.cpp` compiles to 245 bytes versus retail 251, with 16 relocations and 77 non-relocation differences. The first code difference is the local frame: retail reserves 0x14 bytes, ours 0x10. Both still use EBX for the return buffer, EBP for the name and EDI for the table through the first lookup. A full STLport `const_iterator` does reserve 0x14, but moves the owner into ESI, materializes the table pointer too early, and worsens the probe to 253/251 with 121 differences. A fallback-only iterator emits 250/251 with 142 differences. No row or pin changed; the original source-level reason for the extra four-byte local remains unknown.
