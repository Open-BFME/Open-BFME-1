# `ParticleSystem::loadPostProcess` at 0x005C5100

* **Owner and slot.** The only vftable holding 0x005C5100 is 0x0110FE48, whose
  slot 0 is `??_GParticleSystem`. It sits in slot 1, BFME's Snapshot
  loadPostProcess slot (slot 2 of these tables returns the snapshot name,
  slot 3 is xfer).
* **Body.** Zero Hour's ParticleSystem::loadPostProcess: for the slave and
  then the master system, if the saved ID is set, throw if the pointer is
  already set, look the system up through TheParticleSystemManager, and
  throw if it is missing or destroyed (+0x1A8).
* The previous row, `Rva005C5100Owner::validateParticleSystems`, described the
  same body under an address-derived owner.
