# `AIGuardRetaliateMachine::loadPostProcess` at 0x0015E850

* **Owner and slot.** The only vftable holding 0x0015E850 (through its ILT thunk)
  is 0x01096420, whose slot 0 is `??_GAIGuardRetaliateMachine`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's AIGuardRetaliateMachine::loadPostProcess is empty, declared
  protected, and `game/GameEngine/Source/GameLogic/AI/AIGuardRetaliate.cpp` already carries that body.
