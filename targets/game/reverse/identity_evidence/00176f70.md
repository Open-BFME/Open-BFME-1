# AIAttackMeleeApproachState::onEnter at 0x00176F70

The retail vtable at VA `0x0109A6C0` has its slot 4 (`+0x10`) pointed through
ILT thunk `0x00011667` to body `0x00176F70`. Slot 0 reaches the matched
`AIAttackMeleeApproachState` deleting destructor at `0x00185450`. The matched
`AttackMeleeStateMachine` constructor at `0x00180EE0` constructs this state,
uses the `AIAttackMeleeApproachState` literal, and installs `0x0109A6C0`.
These independent constructor and destructor witnesses establish the owner.

The matched `AIInternalMoveToState` base vtable at `0x01095B08` has
`AIInternalMoveToState::onEnter` in the same slot 4. The target calls that base
body through ILT `0x00021E27`; its `thiscall`, no-stack-argument shape and plain
`ret` establish the override ABI. The C++ probe emits the override symbol
`?onEnter@AIAttackMeleeApproachState@@UAE?AW4StateReturnType@@XZ` at 747/747
bytes with 32 aligned relocations. Six register-allocation bytes still differ
(`+0x1E7`, `+0x206`, `+0x20C`, `+0x2A2`, `+0x2A8`, `+0x2AE`), so this is a
banked partial, not a matched source claim. The prior bank had the same six
differences with an opaque owner; the revised bank records the proven identity.
