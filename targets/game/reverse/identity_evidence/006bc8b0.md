# `W3DGhostObject::loadPostProcess` at 0x006BC8B0 and `W3DRenderObjectSnapshot::loadPostProcess` at 0x006BCDF0

* **0x006BC8B0 owner.** Its only reference is ILT 0x00016216, held in slot 1 of
  vftable 0x0111CFB8. Slot 0 of that table reaches `??_GW3DGhostObject`
  (0x006BD070) and slot 2 is the class-name getter 0x006BC720, which returns the
  literal "W3DGhostObject" at 0x0111CFCC.
* **0x006BCDF0 owner.** Its only reference is ILT 0x00019AE7, held in slot 1 of
  vftable 0x0111D024. The constructor 0x006BCD80 installs that table, and its
  slot-2 getter 0x006BCE00 returns the literal "W3DRenderObjectSnapshot" at
  0x0111D038.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot 1
  (targets/game/reverse/identity_evidence/001b3dd0.md).
* **Bodies.** Both are a lone `ret`. Zero Hour's W3DGhostObject::loadPostProcess
  calls the out-of-line GhostObject::loadPostProcess (0x001B3DD0), which compiles
  to a 5-byte `jmp`; BFME's override therefore does not extend the base.
* The previous rows put the snapshot's name on 0x006BC8B0 by an elimination tie
  and left 0x006BCDF0 as `?Rva006BCDF0@@YAXXZ`.
