# `DX8Wrapper::Compute_Caps` at 0x00906E70

* **Caller.** Its only caller is DX8Wrapper::Do_Onetime_Device_Dependent_Inits (0x0090ADC0), which in Zero Hour dx8wrapper.cpp calls Compute_Caps(D3DFormat_To_WW3DFormat(DisplayFormat)).
* **Body.** The source is Zero Hour's Compute_Caps character for character (whitespace and comments aside).
* The C++ in `game/Libraries/Source/WWVegas/WW3D2/dxwrapper.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
