# MapSelectorTooltip at 0x004F1760

The 267-byte body at 0x004F1760 was lifted as a naked `__emit` copy under the
truncated name `?getStartingCash@SkirmishPreferences@@`. The body does not do that
job. It is the map-preview tooltip callback `MapSelectorTooltip(GameWindow *,
WinInstanceData *, UnsignedInt)` from Zero Hour's SkirmishGameOptionsMenu.cpp.

Evidence:

- String literals. It looks up the image named `Cash` (0x011027FC) through
  `TheMappedImageCollection->findImageByName` (ILT 0x0001D606 -> 0x005D2CF0). It
  fetches `TOOLTIP:SupplyDock` (0x011027E4) through `TheGameText` vtable slot
  0x28 and passes it to `Mouse::setCursorTooltip` (ILT 0x000346E9). These are
  exactly the supply-dock branch of the ZH twin. BFME has no tech-building
  branch here: there is no `TecBuilding` lookup and no `TOOLTIP:TechBuilding`.
- Shape. It splits `mouse` into `x = mouse & 0xFFFF` and `y = mouse >> 16`
  (LOLONGTOSHORT/HILONGTOSHORT) and calls `window->winGetScreenPosition`. It
  then walks the STL list at 0x012F15B4, which is
  `TheSupplyAndTechImageLocations.m_supplyPosList` at +4 of the object at
  0x012F15B0, and tests a 15-pixel square (SUPPLY_TECH_SIZE) per entry.
- Installers. ILT 0x00037EF2 (the gen-thunk row targeting 0x004F1760) is
  pushed as the argument to `GameWindow::winSetTooltipFunc` (ILT 0x0003D127 ->
  0x00478EB0) at 0x004CBF89. That site is inside the matched
  `InitLanGameGadgets` at 0x004CBBB0, whose landed source calls
  `windowMap->winSetTooltipFunc(MapSelectorTooltip)`, as ZH
  LanGameOptionsMenu.cpp:730 does. It is pushed again at 0x004F30BF inside the
  dump at 0x004F2C00, which corresponds to ZH SkirmishGameOptionsMenu.cpp:1108.

Probe: 267/267 bytes, exact outside relocations. All 5 REL32 sites resolve to
retail's targets. The C++ is a port of the ZH twin with the tech branch
removed. It has no inline assembly and no emitted bytes.
