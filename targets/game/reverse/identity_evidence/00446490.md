# `InGameUI::deselectDrawable` at 0x00446490

* **Slot.** Slot 57 (`+0xE4`) of InGameUI's primary vftable 0x00CF5B38
  (installed by `??0InGameUI`, 0x0044B800) is the ILT thunk 0x0000396D, which
  jumps to 0x00446490. `InGameUIDeselectAllDrawables.cpp` (matched) declares
  `deselectDrawable` in that slot and calls it per selected drawable, as Zero
  Hour's `deselectAllDrawables` does; `Drawable::setSelectable` (matched over
  its full 74 bytes) calls `TheInGameUI` through `+0xE4` when a drawable stops
  being selectable, as Zero Hour's does.
* **Body.** It is Zero Hour's `deselectDrawable`: it tests the selected byte at
  `Drawable+0x3AC`, stamps the frame, clears the flag, finds and erases the
  drawable from `m_selectedDrawables` (`+0x18`, `name_oracle` 1.00),
  decrements the select count, calls `evaluateSoloNexus(NULL)` and notifies
  `TheControlBar`. BFME appends a reset of 25 entries at `+0x44`.
* **Flag clear.** The call after the frame stamp goes to 0x00417240, carved as
  `Rva00417240::run`: it tests and clears the same `+0x3AC` byte, Zero Hour's
  `friend_clearSelected` shape. It stays under its address-derived ledger
  name; the bank's `BfmeDeselectFlagView` was a view with an
  `/alternatename` onto the ILT thunk, not an identity.
