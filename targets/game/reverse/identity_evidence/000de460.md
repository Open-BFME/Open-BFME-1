# String/vector record assignment at 0x000DE460

The full 33-byte body assigns its first member through StringBase<char>::set
at 0x00887C90. It then passes source+4 and destination+4 to ILT 0x00007A63,
which jumps to the 212-byte vector<AsciiString> assignment at 0x000DE2C0.
That callee checks self-assignment, reads three vector pointers, computes element
counts with four-byte stride, and copies or constructs strings through the
matched vector helpers. The independently matched Rva0076F980 assignment and
LivingWorldRegion method at 0x0061A860 also use this exact vector assignment
contract. No new callee pin is necessary.

The old MixFileFactoryClass::AddInfoStruct claim would assign a StringClass
at +0; the full gate rejects that first call because retail reaches the owning
StringBase<char> implementation instead. The record's enclosing semantic
identity is not established. Use Rva000DE460Record with canonical AsciiString
at +0 and STLport vector<AsciiString> at +4; do not infer MixFile ownership.

The replacement preserves the 33-byte range and returns the destination after
the two assignments. This repairs a failed existing claim, not new coverage.
