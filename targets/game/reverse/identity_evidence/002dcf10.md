# 0x002DCF10 identity correction

The matched row previously called this body `bfmeNotify1177` and its callee at
0x00851910 `BfmeQ1177::bfmeSend1177`. Those names had no retail evidence.

The independent Weapon `FieldParse` table at 0x0109FE18 maps the token
`LuaEventNugget` to 0x001E6240. That matched caller allocates the object whose
address-derived type is `Made001E6240`, then calls 0x002DCF10 with the INI
reader and that object as its first two arguments. The caller-side relocation
therefore establishes the already-pinned address-derived name
`q4Notify001E6240(void *, Made001E6240 *, int, int)` without claiming an
unproved semantic helper name.

The 0x002DCF10 body independently confirms the contract. It constructs a
0x84-byte local with the exact `MultiIniFieldParse` layout (sixteen field-table
pointers, sixteen offsets, then count), calls the matched
`MultiIniFieldParse::add` at 0x00850920 with table 0x010CED70, reloads its first
argument into ECX, and calls 0x00851910 with the second argument plus the local.
The latter callee is `INI::initFromINIMulti`, proven by the matched INI caller
at 0x008520A0 and by this same ABI across the Weapon parser family. Its body
reads INI fields, tokenizes lines, scans `MultiIniFieldParse`, and throws
INI exceptions; it cannot be the unrelated invented Bfme method.

The target ends at the plain `ret` at 0x002DCF5B (76 bytes). The corrected C++
retains the four-argument cdecl ABI visible at the 0x001E6240 call site; the
last two arguments are unused, as retail shows.
