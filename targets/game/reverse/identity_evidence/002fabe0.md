# ScriptActions::doUnitSetModelConditionForDuration at 0x002FABE0

The 284-byte body at 0x002FABE0 was claimed as
`?doUnitSetModelConditionForDuration@ScriptActions@@IAEXABVAsciiString@@0MH@Z`,
whose fourth parameter was declared `Int percent` with no evidence. This file
records why the parameter is a `Real` and the row is renamed to
`?doUnitSetModelConditionForDuration@ScriptActions@@IAEXABVAsciiString@@0MM@Z`.

- **Callee reads it as a float.** The body forwards its fourth argument as the
  third argument of `?rva001D02C0@Object@@QAEXHHM@Z` (0x001D02C0, matched,
  via ILT 0x00049D73). That body does `fild` of the member count, then
  `fmul dword ptr [esp+0x2c]` at +0x72; with esp = entry-0x20 at that point
  (three EH pushes, `sub esp,8`, esi/edi/ebx pushes) the operand is entry+0xC,
  the third argument. So the forwarded value is a `Real`.
- **Caller supplies a Real.** executeAction (0x00303BF0) case 434 at +0x5C47
  loads `Parameter[3]+0xC`, the same slot it reads for the `Real` duration
  from `Parameter[2]+0xC`, and pushes it as the fourth argument.
- **Callee binding.** The TU now calls the landed `Object::rva001D02C0`
  (HHM) instead of the TU-local `Rva001D02C0Object::callRva001D02C0` (HHH)
  pin, which gave 0x001D02C0 a second name with a contradicting ABI.
- The type only changes a dword copy; the TU probes EXACT 284/284 under the
  new name.
