# `ScoreKeeper::loadPostProcess` at 0x000E9B00

* **Owner and slot.** The only vftable holding 0x000E9B00 (through its ILT thunk)
  is 0x01084df0, whose slot 0 is `??_GScoreKeeper`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's ScoreKeeper::loadPostProcess is empty, declared
  protected, and `game/GameEngine/Source/Common/RTS/ScoreKeeper_loadPostProcess.cpp` already carries that body.
