# 0x0026A2E0 SpecialPowerModule::initiateIntentToDoSpecialPower (signature corrected)

Old ledger name: `?initiateIntentToDoSpecialPower@SpecialPowerModule@@IAE_NPBVObject@@PBUCoord3D@@PBVWaypoint@@I@Z` (Zero Hour signature carried by the Open-BFME5 __emit lift).

New name: `?initiateIntentToDoSpecialPower@SpecialPowerModule@@IAEXPBVObject@@PBUCoord3D@@II@Z`.

## Method identity (unchanged)
- Zero Hour twin `GeneralsMD/.../SpecialPower/SpecialPowerModule.cpp:430`: loop over `getObject()->getBehaviorModules()`, `getSpecialPowerUpdateInterface()`, call the update interface's slot-0 `initiateIntentToDoSpecialPower(template, targetObj, targetPos, ...)`, set valid and break. Retail 0x0026A39B..0x0026A3FE is that loop (Object+0x1F0 m_behaviors, BehaviorModuleInterface at module+0x0C slot 24 +0x60, spu slot 0 called with modData+0x08 template and the four incoming arguments).
- Callers: landed `SpecialPowerModuleInterface::doSpecialPower` 0x0026A550, `doSpecialPowerAtObject` 0x0026A5B0, `doSpecialPowerAtLocation` 0x0026A620, `doSpecialPowerUsingWaypoints` 0x0026A690 (vtable slots 11-14) and d_0025f6c0 call it through ILT 0x000361AB at the point Zero Hour calls initiateIntentToDoSpecialPower.

## Why the decorated name changes
- Return: no path sets `al` before `ret 0x10`; the !valid path jumps from +0x146 straight to the epilogue at +0x1D3 and the valid path's last call is `StringBase<char>::releaseBuffer` (+0x1CE). Verified against `tools/dis_retail.py 0x0026A2E0 488`: eax is untouched between the last call and the epilogue at +0x1D3-+0x1E5. BFME returns `void`, not `Bool`.
- Third argument: `test dword ptr [esp+0x38], 0x40000` at +0x187 tests it against the same bit each caller tests on its own `commandOptions` before passing that value in this slot; it is `UnsignedInt commandOptions`, not `const Waypoint *`.
- Fourth argument: only forwarded to spu slot 0 (+0x10A); callers pass 0 or their own UnsignedInt. Kept as `UnsignedInt`, no identity claimed.
- `protected` kept from the Zero Hour declaration; `struct Coord3D` kept (PBU) as in Zero Hour and the callee pins.

The body byte-matches 488/488 (probe EXACT modulo relocations) as clean C++ in `SpecialPowerModule_initiateIntentToDoSpecialPower.cpp`.
