# GameLODManager identity at 0x0007EEB0

The body at `0x0007EEB0` is not
`GameLODManager::applyStaticLODLevel`.

`GameLODManager::setStaticLODLevel` at `0x0007EDF0` calls ILT
`0x00036A11`. `tools/pin_consistency.py` independently routes that thunk to
the distinct 868-byte body at `0x0007E9B0`, proving the location of
`applyStaticLODLevel`.

The body at `0x0007EEB0` references `Data\\INI\\GameLOD.ini`,
`Data\\INI\\GameLODPresets.ini`, `Benchmark.txt`, and
`BenchProfile = %s %d %f %f %f`. Its 19 resolved direct calls include the INI
constructor, destructor, and two `INI::load` calls, plus `fopen`, `fprintf`,
and `fclose`. Those anchors match the `GameLODManager::init` flow in
`GameLOD.cpp`. The method saves incoming `ECX` in `ESI` and makes its later
member accesses through `ESI`, establishing the member receiver.

The 1063-byte Ghidra extent is truncated: its nominal end, `0x0007F2D7`,
falls in the middle of the seven-byte `mov dword ptr fs:[0], ecx` at body
offset `+0x423`. The complete epilogue continues with `pop ebx`,
`add esp, 0x888`, and a plain `ret` at `+0x431`; `int3` padding begins at
`0x0007F2E2`. The complete boundary is therefore
`[0x0007EEB0, 0x0007F2E2)`, 1074 bytes. The plain return proves the
no-argument member ABI expected by `GameLODManager::init`; the current BFME
initialization body and its 0x87C-byte EH frame are not yet reconstructed
closely enough to claim a C++ match.
