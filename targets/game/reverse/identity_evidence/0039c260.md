# LightPointSystem::rva0039C260 at 0x0039C260

The 287-byte body at 0x0039C260 was claimed as
`?bfmeRunEUAe@BfmeSpecialPowerAllowanceStore@@QAEXPAVBfmePlayerSpecialPowerState@@PAX@Z`.
Its only support was the Open-BFME5 pin note on ILT 0x00045039 ("forwards ...
to the global allowance store") and the shape-only pin at ILT 0x00024E33; no
data pin names the global `TheBfmeSpecialPowerAllowanceStore`. Retail refutes
the class. This file records the evidence for the address-derived replacement
`?rva0039C260@LightPointSystem@@QAEXPAVBfmeUniqueIntegerStoreC8C0@@PAX@Z`.

- **`this` is the LightPointSystem.** The caller 0x000C9930 loads
  `ecx = [0x012F0FF8]`. The matched `INI::parseLightPointLevel` (0x0039CCF0,
  the INI block registry names the LightPointLevel block) loads that same
  global (+0x0E, +0x4E, +0x140) and at +0x147 calls `LightPointSystem::addLevel`
  (ILT 0x0001FE88 -> 0x0039CAB0) on it, which walks the vector at this+8 whose
  Overridable entries carry an AsciiString name at +0x0C. This body walks the
  same vector, so its entries are LightPointLevels.
- **Twin.** The matched `?rva0039CB60@LightPointSystem@@QAEXPAVBfmeUniqueIntegerStoreC8C0@@ABVAsciiString@@@Z`
  runs the same loop and is called by 0x000C9900 with the identical
  `ecx = [0x012F0FF8]`, `this+0x274` argument pair; it adds the found level's
  index to that store, this body erases it from the store's vector at +0x08.
  The store keeps the twin's address-token class `BfmeUniqueIntegerStoreC8C0`.
- **Name.** The method is not proven, so it keeps the address token
  (AGENTS.md 3a). The ILT 0x00045039 pin and the caller BfmeItemEUARunE.cpp
  now spell this name, so the body keeps one identity. The TU moves from
  GameLogic/Object/SpecialPower to GameClient next to the twin.
- The TU probes EXACT 287/287 under the new name; the caller 0x000C9930
  probes EXACT 31/31.
