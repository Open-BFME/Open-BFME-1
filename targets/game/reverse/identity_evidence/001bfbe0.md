# Object::getGroup at 0x001BFBE0 and the shroud forwarder at 0x008F7430

| body | old row | identity |
|---|---|---|
| `0x001BFBE0` | `Player::getRankLevel` | `Object::getGroup` |
| `0x0026FB30` | `Gen_0026fb30::m` (scaffold) | `AIUpdateInterface::getGroup` |
| `0x008F7430` | `AIUpdateInterface::getGroup` and `PartitionManager::getShroudStatusForPlayer` | `PartitionManager::getShroudStatusForPlayer` only |

`0x001BFBE0` is `mov eax,[ecx+0x188]; ret`, reached through ILT
`0x000382C1`. Its only two callers settle it:

1. `AIUpdateInterface::chooseGoodLocomotorFromCurrentSet` (0x00270D40)
   stores the new locomotor at `+0x1CC`, then loads `[esi+8]`, the unit's
   `Object`, calls this body, tests the result, calls it again on the same
   `Object` and sets byte `+0x10` of the result to 1, then clears a flag on
   the locomotor. That is Zero Hour's
   `if (getGroup()) getGroup()->recomputeGroupSpeed();` followed by
   `m_curLocomotor->setUsePreciseZPos(FALSE)`, with
   `recomputeGroupSpeed()` being `m_dirty = true`. The receiver is an
   `Object`, not a `Player`.
2. `0x0026FB30` is `mov ecx,[ecx+8]; jmp 0x000382C1`, which is Zero Hour's
   `AIGroup *AIUpdateInterface::getGroup() { return getObject()->getGroup(); }`.

`0x008F7430` is `mov ecx,[ecx+0xC]; jmp 0x008F9550`. The jump target takes
a player index, rejects anything outside 0..15, looks up a grid cell from a
`Coord3D` and returns a shroud status 0, 1 or 2 with `ret 8`
(`Gen_008F7CD0::bfmeCheckFD`). Object::getGroup takes no arguments, so the
body cannot be `AIUpdateInterface::getGroup`. All 16 named callers are
shroud checks (FX, in-game UI, action manager, audio), matching the pin
`PartitionManager::getShroudStatusForPlayer`.

The pin `?getGroup@Object@@` at `0x008F9550` was derived from the false
`AIUpdateInterface::getGroup` claim and is removed; the new
`Object::getGroup` row supplies its call targets.
