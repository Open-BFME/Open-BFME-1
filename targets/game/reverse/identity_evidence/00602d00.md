# `W3DTerrainVisualBase::loadPostProcess` at 0x00602D00

* **Owner and slot.** The only vftable holding 0x00602D00 (directly or through its
  ILT thunk) is 0x011150b8, whose slot 0 is `??_GW3DTerrainVisualBase`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in
  slot 1: the matched loadPostProcess bodies of AIAttackState (154 B),
  Drawable, DockUpdate, BuildListInfo and Radar (which calls its slot-4
  refreshTerrain) are each held in slot 1 of their class's table.
* **Body.** A lone `ret`. W3DTerrainVisualBase is BFME's name for Zero Hour's
  TerrainVisual (its slot-3 xfer, 0x00602C40, is TerrainVisual::xfer), whose
  loadPostProcess is declared protected and is empty.
* The previous row, `?Rva00602D00Noop@@YAXXZ`, carried no identity.
