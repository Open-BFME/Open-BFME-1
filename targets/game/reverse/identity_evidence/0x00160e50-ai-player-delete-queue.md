# 0x00160E50: deleteQueue(TeamInQueue *)

The matched row currently calls this 15-byte body `LocomotorTemplateDeleteAccess::destroy`, a helper invented by our `Locomotor.cpp` reconstruction. The retail address is the AIPlayer queue-deletion callback:

- Retail `AIPlayer::clearTeamsInQueue` at `0x00160E70` pushes callback VA `0x00560E50` twice, before calls to the matched `AIPlayer::removeAll_TeamBuildQueue` at `0x001607C0` and `AIPlayer::removeAll_TeamReadyQueue` at `0x00160990`. Retail `AIPlayer::~AIPlayer` at `0x001613C0` independently passes that same callback VA to the same two helpers.
- Each queue-removal helper calls its callback with one `TeamInQueue *` argument: `push eax; call ebx; add esp,4`, establishing a `void __cdecl (*)(TeamInQueue *)` ABI. Native and vendored `AIPlayer.cpp` both declare `static void deleteQueue(TeamInQueue *o)` at file scope and pass it to exactly these two helpers. The `static` declaration gives internal C++ linkage; a scratch MSVC 7.1 compile emits `?deleteQueue@@YAXPAVTeamInQueue@@@Z` for this definition.
- The 15-byte retail callback loads its one argument from `[esp+4]`, null-checks it, then calls its vtable slot zero with argument `1`, returning with plain `ret`. This is the virtual scalar-deleting-destructor form. The matched `TeamInQueue` vtable at `0x00C96940` routes slot zero to the scalar deleting destructor at `0x00161260`, whose complete destructor is at `0x00160ED0`. The BFME source form `if (o) delete o;` on a virtual `TeamInQueue` class reproduces those 15 bytes exactly.
- Retail `LocomotorStore::~LocomotorStore` at `0x005D2C10` inlines its LocomotorTemplate virtual deletion at `+0x40`; it does not call `0x00160E50`. The `LocomotorTemplateDeleteAccess` class exists only in the reconstructed source, not in the vendored source. Byte similarity alone cannot make its helper the authentic identity of this AIPlayer callback.

Before changing a production row, a scratch copy of `AIPlayerQueueTeardown.cpp` with a real virtual `TeamInQueue` destructor declaration and `static deleteQueue` definition probed `EXACT` at `0x00160E50` (15/15 bytes, zero relocations). The same scratch TU reprobed `AIPlayer::clearTeamsInQueue` at `0x00160E70` as `EXACT` (27/27 bytes, four relocations). The correction must preserve the 15-byte extent, retire the generic matched row through `add_match.py --correct-identity`, and gate every matched row in the affected source files.
