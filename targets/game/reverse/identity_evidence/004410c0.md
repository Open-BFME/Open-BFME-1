# InGameUI::update at 0x004410C0

The lift row named this body `?update@InGameUI@@`, a truncated decoration with
no signature, and pointed it at a naked `__emit` copy
(`InGameUIUpdateThunk.cpp`, object symbol `_bfme_InGameUI_update_4410C0`).
The method is the same; only the decoration is completed, to
`?update@InGameUI@@UAEXXZ` (virtual, thiscall, no arguments, void).

- Vtable: the InGameUI table 0x010F5B38, installed by the InGameUI
  constructor at 0x0044B800, holds ILT 0x0040750E at slot 5 (+0x14), and that
  thunk jumps to 0x004410C0. Slot 5 is SubsystemInterface::update in BFME's
  subsystem tables (TheControlBar, TheBannerUI and TheAptPalantir are all
  updated through +0x14 inside this same body).
- Body: it follows Zero Hour's InGameUI::update statement by statement: movie
  stream polling, the six-entry UI message fade loop (m_messageDelayMS at
  +0x858, GameGetColorComponents, removal inlined), the ControlBar.wnd
  MoneyDisplay/PowerWindow statics and GUI:ControlBarMoneyDisplay text,
  updateFloatingText, TheControlBar update, the m_windowLayouts walk and the
  keyboard camera rotate block driven by GlobalData m_keyboardCameraRotateSpeed.
  The military-subtitle block is out of line at 0x0043E700.
- Ends in `ret` with no stack cleanup, which fits `UAEXXZ`.

The C++ in `game/GameEngine/Source/GameClient/InGameUI.cpp` reproduces all
1234 bytes. The range is the same as the lift row's, so no bytes are newly
covered.
