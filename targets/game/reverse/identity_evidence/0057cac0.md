# Skirmish profile refresh at 0x0057CAC0

Matched `BfmeAptScreenSkirmish::personaAccept` calls the profile refresh method,
and the retail body fetches the `APT:NewProfile` and `APT:DeleteProfile` labels.
The 456-byte native body reproduces the retail instructions and all 24 relocation
sites; the scoped build verifies the claim.

The established `ProfileList` name remains the semantic type. The accessor at
0x0009F820 copies the member at host +0x14 by calling 0x0009F6E0, a list copy
constructor. That constructor calls 0x0009F340, whose matched source and pin
identify STLport `list<UnicodeString>` insertion. The existing accessor source
emits the address-derived return type `Rva0009F820String`. The new callback
keeps `ProfileList` as the list base and uses the address-derived derived class
only to meet that already matched accessor's C++ symbol. This is the one
`ProfileList` to `Rva0009F820String` substitution the name-history check sees;
the descriptive type and its behavior are retained.

The last code-shape difference was a dead `this` value on the call to
`StringBase<unsigned short>::compareNoCaseRaw` at 0x0009ECA0. The matched
helper reads only its three stack arguments, never ECX. Retail loads ECX from
the caller-frame address at +0x2C, while the earlier reconstruction loaded the
profile's address at +0x1C. Passing the former address changes no data read by
the helper and reproduces retail's final byte. All other direct callees have
ledger names; the three newly added thunk pins are verified by
`pin_consistency.py --routes` against their matched physical bodies.
