# 0x009F4D80 node insertion

Retail has one 181-byte body at `0x009F4D80..0x009F4E35`, ending in `ret 4`. The matched `Gen009F5040::handle` at `0x009F5040` calls it at `+0x7F` with `this` in ECX and its 0x30-byte node in the sole stack argument, immediately after the matched `Gen009F5040::remove` call at `+0x77`. The matched `BfmeThing912C::bfmeGo912C` at `0x009F4F80` also calls the same body at `+0x13`, so a unique original class or method spelling is not established by that shared target. The new row therefore keeps the RVA in `Gen009F5040::linkNode_009F4D80` and does not change the older `bfmeDo912C` or `bfmeLinkEQR` aliases.

The body's only direct REL32 call is at `+0x19` to `0x009F4900`; the callee takes `this` in ECX and four stack arguments and returns with `ret 0x10`. This is the `calculate` helper already declared and routed by the matched `Gen009F5040::handle` source. The subsequent virtual call through the node's item uses vtable offset `+0x1C`; the result is clamped to `[-1,15]`. The counter walk indexes the existing bucket layout, increments the selected counter, and follows bitmasked child counters. The final writes at `+0x97`, `+0x9E`, and `+0xA5` specifically use node offsets `+0x10/+0x14`: the secondary intrusive links already represented in the matched `remove` source. Substituting the primary links at `+0x18/+0x1C` leaves precisely those three operand bytes wrong.

The authored C++ scratch probe emitted 181/181 bytes with one masked call relocation. The strict scoped byte and relocation gate is required before the row is claimed; the byte-shape result alone is not evidence of the call target or semantic name.
