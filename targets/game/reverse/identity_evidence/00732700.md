# W3DTreeBuffer::doLighting access specifier at 0x00732700

The 470-byte body at 0x00732700 keeps the identity it landed with,
W3DTreeBuffer::doLighting; only the access letter of its decorated name
changes, from public `QBE` to protected `IBE`. The bytes cannot distinguish
public from protected, so the access is taken from the only identity evidence
the row cites: the Zero Hour twin declares doLighting `protected:` in
GeneralsMD W3DTreeBuffer.h (lines 266-274), in the same block as updateSway and
loadTreesInVertexAndIndexBuffers. The sibling ledger rows already follow the
twin: W3DTreeBuffer::updateSway at 0x00732500 is `IAE` and
W3DShrubBuffer::doLighting at 0x0071D280 is `IBE`. Its caller at 0x00734CB0,
loadTreesInVertexAndIndexBuffers, is itself protected in the twin and a
declaration copied from the twin references the `IBE` spelling.

Same source body, same address and same 470-byte extent. Probe of
`?doLighting@W3DTreeBuffer@@IBEIPBVVector3@@PBURva00732700Rgb@@01II@Z`:
470/470 bytes, three relocation slots, exact outside relocations.
