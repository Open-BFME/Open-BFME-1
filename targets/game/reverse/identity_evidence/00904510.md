# `DX8Wrapper::Set_Vertex_Buffer(const DynamicVBAccessClass&)` at 0x00904510

* **Caller.** Its callers include BaseHeightMapRenderObjClass::renderShoreLines (0x006CCE70) and W3DSmudgeManager::render (0x00722BD0), both of which call DX8Wrapper::Set_Vertex_Buffer(vb_access) with a DynamicVBAccessClass in Zero Hour.
* **Body.** The source is Zero Hour's DynamicVBAccessClass overload character for character (whitespace and comments aside).
* The C++ in `game/Libraries/Source/WWVegas/WW3D2/dxwrapper.cpp` has always compiled under this symbol (the row's
  object-symbol note); only the ledger row carried the `?d_` placeholder.
