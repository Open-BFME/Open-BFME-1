# LadderDefs parseLadder at 0x0062AD50

The static `parseLadder(AsciiString)` definition in `LadderDefs.cpp` is the
identity of the retail body at `0x0062AD50`.

The matched `LadderList::checkLadder` caller at `0x0062BC60` constructs a
by-value `AsciiString`, calls this body directly at `+0xDC`, and consumes the
returned pointer as a `LadderInfo *`. The retail parser contains the complete
ladder grammar literal family, including `<Ladder `, `RandomMaps `,
`RandomFactions `, `Faction `, `MinWins `, `MaxWins `, and `CryptedPass `.
The existing semantic pin independently records the same static function.

The body has a continuous 3077-byte extent. Its final cleanup block ends with
a jump from `+0xC00` back to the shared return epilogue, and `int3` padding
begins at `0x0062B955`. Its by-value string argument and pointer return agree
with the source signature. All 35 direct callees resolve.

`name_oracle` has no BFME witness for `LadderInfo`, so this evidence does not
infer its private member names. It does witness `LadderList`'s three list
members at offsets `+0`, `+4`, and `+8`, consistent with the two matched
callers that feed this file-local parser.
