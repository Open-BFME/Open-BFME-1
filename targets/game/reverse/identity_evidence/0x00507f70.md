# 0x00507F70 populateQuickMatchMapSelectListbox: a BfmeAptScreenQuickMatchMenu member, not a free function

The ledger row `?populateQuickMatchMapSelectListbox@@YAXAAVQuickMatchPreferences@@@Z` came with the Open-BFME5 `__emit` lift. Its decoration says `__cdecl` free function. The retail body does not have that ABI.

## ABI

- The prologue at +0x1C does `mov edi, ecx` and the body reads `[edi+0x23C]`, `[edi+0x244]` and `[edi+0x284]`.
- The epilogue at +0x2A9 is `ret 4`: `__thiscall` with one stack argument.
- Both callers go through ILT `0x00006848`:
  - `0x005091F0+0x69F` (the init-gadgets body: strings `CustomMatch_selected`, `WOLQuickMatchMenuFade`, `WOLQuickMatchMenu.wnd:StaticTextTitle`): `lea edx,[esp+0x24]; push edx; mov ecx,esi; call`.
  - `WOLQuickMatchMenuSystem 0x0050A470+0x337`: `lea ecx,[pref]; push ecx; mov ecx,esi; call`.

## Owner

The matched constructor `??0BfmeAptScreenQuickMatchMenu@@QAE@PAX@Z` (0x00505830) keys these windows by name:

- `+0x23C` is `WOLQuickMatchMenu.wnd:ComboBoxNumPlayers`.
- `+0x244` is `ComboBoxLadder`.
- `+0x284` is `ListBoxMapSelect`.

Those are exactly the three windows ZH's helper uses: `comboBoxLadder`, `comboBoxNumPlayers` and `listboxMapSelect`. The sibling helper `populateQMColorComboBox` (0x00505DB0) already compiles as `?populateQMColorComboBox@BfmeAptScreenQuickMatchMenu@@QAEXAAVQuickMatchPreferences@@@Z`.

## Method

The ZH twin is `GeneralsMD/.../Menus/WOLQuickMatchMenu.cpp:560`. The body follows it statement for statement and byte-matches 684/684, including the `isMapSelected` call to 0x000ABD90, whose matched row already names this function as its caller.

## Corrected name

`?populateQuickMatchMapSelectListbox@BfmeAptScreenQuickMatchMenu@@QAEXAAVQuickMatchPreferences@@@Z`
