# INI declaration predicate at 0x00851BF0

The existing exact 510-byte line filter is called by matched INI::load in
INILoadBFMERetail.cpp with two owning string values and a mutable input buffer.
The native INI source (ini.cpp, INI::isDeclarationOfType) and the vendored
Generals INI.cpp define the same static bool function with two by-value
AsciiString arguments and char*. All checks, strict strlen comparisons,
case-insensitive comparisons, temporary terminator restoration, and trailing
whitespace loop agree with the retail control flow. INI.h declares it static.

Replace the local BFMERetailAsciiString copy with canonical ascii_string.h and
StringBase<char> method definitions already witnessed by string_base.cpp.
TU-local inline definitions reproduce retail visibility; no shared header changes.
The four cleanup calls reach StringBase<char>::releaseBuffer at 0x00887940.
The emitted INI static method is exactly 510 bytes outside relocation operands.
The scoped gate verifies those operands and literal references before landing.

The existing address-derived free-function pin remains usable by the older
INI::load translation unit: static member and free cdecl entry ABIs are the
same. This change adds no pin and does not invent a different retail function.
It preserves the same 510-byte range; this is identity and readability work,
not a newly converted body or an increase in matched bytes.
