# 0x0027EC80: AIUpdateModuleData::parseLocomotorSet

The row was an Open-BFME5 `__emit` lift named `?findLocomotorTemplateVector@AIUpdateModuleData@@`. That name is wrong. The body is the INI field parser `AIUpdateModuleData::parseLocomotorSet(INI*, void*, void*, const void*)`.

Evidence:
- The three INIException format strings it throws are VA 0x010BA738 `Attempted to specify a locomotor for object %s without an AIUpdate block.`, VA 0x010BA6F4 `re-specifying a LocomotorSet is no longer allowed\n` and VA 0x010BA6D4 `Locomotor %s not found!\n`. It also compares against `None` at VA 0x01082EEC. These are exactly the messages of the Zero Hour twin `AIUpdateModuleData::parseLocomotorSet` (inputs/reference/CnC_Generals_Zero_Hour/GeneralsMD/Code/GameEngine/Source/GameLogic/Object/Update/AIUpdate.cpp:165).
- It calls the twin's callees in the twin's order: ThingTemplate::friend_getAIModuleInfo (0x0013FB60 via ILT 0x00030A44), INI::getNextToken, INI::scanIndexList with the name table at VA 0x012B1220 (SET_NORMAL..SET_COMBO3, NULL), LocomotorTemplateMap::operator[] (0x00147070 via ILT 0x0000441C) for the empty check, clear and push_back, _stricmp, NameKeyGenerator::nameToKey, LocomotorStore::findLocomotorTemplate (0x001B7040), vector::_M_insert_overflow (0x00141F70) and INI::getNextTokenOrNull.
- The receiver global VA 0x012EF504 is the one GameEngine init registers under the string `TheLocomotorStore` (code at 0x00079A15).
- A clean port of the ZH twin byte-matches all 412 bytes with 0 non-relocation diffs. MSVC also emits an int3 after the final noreturn throw at +0x19C, and retail has 0xCC there too.

For comparison, the real findLocomotorTemplateVector is a small const map-find accessor with none of these strings or INI calls.
