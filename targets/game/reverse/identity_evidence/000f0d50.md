# `TeamTemplateInfo::loadPostProcess` at 0x000F0D50

* **Owner and slot.** The only vftable holding 0x000F0D50 (through its ILT thunk)
  is 0x01085e70, whose slot 0 is `??_GTeamTemplateInfo`. It sits in slot 1.
* **Slot meaning.** BFME's Snapshot-derived tables put loadPostProcess in slot
  1: the matched loadPostProcess bodies of AIAttackState (154 B), Drawable,
  DockUpdate and BuildListInfo are each held in slot 1 of their table, and
  Radar's (0x00107270) calls its slot-4 refreshTerrain.
* **Body.** A lone `ret`: Zero Hour's TeamTemplateInfo::loadPostProcess is empty, declared
  protected, and `game/GameEngine/Source/Common/RTS/Team.cpp` already carries that body.
