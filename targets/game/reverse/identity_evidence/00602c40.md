# `W3DTerrainVisualBase::xfer` at 0x00602C40

* **Owner and slot.** The only vftable holding 0x00602C40 (through ILT
  0x00040C2D) is 0x011150B8, whose slot 0 is `??_GW3DTerrainVisualBase`
  (0x00602E50, matched). It sits in slot 3, BFME's Snapshot xfer slot (the
  matched xfer bodies of six classes checked are all in slot 3).
* **Caller.** Its only direct caller is W3DTerrainVisual's slot-3 xfer
  (0x007318A0), which calls it first -- the base-class call Zero Hour's
  W3DTerrainVisual::xfer makes to TerrainVisual::xfer.
* **Body.** Version 1 and nothing else: Zero Hour's TerrainVisual::xfer.
  W3DTerrainVisualBase is BFME's name for that base.
* The previous row, `?dup_00602c40@@YAXXZ`, attributed these bytes to
  FXParticleSystem::ButterflyDrawModuleInfo::DoXfer as an identical-code
  alias; retail has no identical-COMDAT folding and no table or call ties the
  address to that class.
