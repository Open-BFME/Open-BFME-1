{
  "schema": 1,
  "sha256": "2821c212db8a3ee575f8820a1d2775c1dbde9966f22f0ea3a263293e2092be9b",
  "symbol": "?parseFontFileName@GlobalLanguage@@SAXPAVINI@@PAX1PBX@Z",
  "score_kind": "author-estimate",
  "score": 0.6,
  "source": "// ?parseFontFileName@GlobalLanguage@@SAXPAVINI@@PAX1PBX@Z\n// partial score=0.6 date=2026-09-16\n// cl: /DNDEBUG /MD /EHsc\n\ntypedef int Int;\n\ntemplate <typename T>\nclass StringBase\n{\n\tfriend class AsciiString;\n\npublic:\n\tStringBase() { m_data = 0; }\n\tvoid set( const StringBase<T> &src );\n\nprivate:\n\tStringBase( const StringBase<T> &other );\n\tvoid releaseBuffer();\n\n\tvoid *m_data;\n};\n\nclass AsciiString\n{\npublic:\n\tAsciiString() {}\n\tAsciiString( const AsciiString &src ) : m_data( src.m_data ) {}\n\t~AsciiString() { m_data.releaseBuffer(); }\n\tconst AsciiString &operator=( const AsciiString &src ) { m_data.set( src.m_data ); return *this; }\n\n\tStringBase<char> m_data;\n};\n\nclass Rva00439370LocalFontEntry\n{\npublic:\n\tRva00439370LocalFontEntry() {}\n\tRva00439370LocalFontEntry( const Rva00439370LocalFontEntry &other );\n\n\tAsciiString m_at00;\n\tAsciiString m_at04;\n};\n\nRva00439370LocalFontEntry::Rva00439370LocalFontEntry( const Rva00439370LocalFontEntry &other )\n\t: m_at00( other.m_at00 ), m_at04( other.m_at04 )\n{\n}\n\nnamespace _STL\n{\n\tclass __new_alloc\n\t{\n\tpublic:\n\t\tstatic void *allocate( unsigned int bytes );\n\t\tstatic void deallocate( void *memory, unsigned int bytes );\n\t};\n}\n\ninline void *operator new( unsigned int, void *place ) { return place; }\ninline void operator delete( void *, void * ) {}\n\nstruct Rva00439370Node\n{\n\tRva00439370Node *m_next;\n\tRva00439370Node *m_prev;\n\tRva00439370LocalFontEntry m_data;\n};\n\nclass Rva00439370List\n{\npublic:\n\tvoid push_front( const Rva00439370LocalFontEntry &value )\n\t{\n\t\tRva00439370Node *pos = m_node->m_next;\n\t\tRva00439370Node *node =\n\t\t\t(Rva00439370Node *)_STL::__new_alloc::allocate( sizeof( Rva00439370Node ) );\n\t\ttry\n\t\t{\n\t\t\tnew ( &node->m_data ) Rva00439370LocalFontEntry( value );\n\t\t}\n\t\tcatch( ... )\n\t\t{\n\t\t\t_STL::__new_alloc::deallocate( node, sizeof( Rva00439370Node ) );\n\t\t\tthrow;\n\t\t}\n\t\tnode->m_next = pos;\n\t\tnode->m_prev = pos->m_prev;\n\t\tpos->m_prev->m_next = node;\n\t\tpos->m_prev = node;\n\t}\n\nprivate:\n\tRva00439370Node *m_node;\n};\n\nclass INI\n{\npublic:\n\tAsciiString getNextAsciiString();\n};\n\nclass GlobalLanguage\n{\npublic:\n\tstatic void parseFontFileName( INI *ini, void *instance, void *store, const void *userData );\n\n\tunsigned char m_unmodelled[ 0x134 ];\n\tRva00439370List m_localFonts;\n};\n\nvoid GlobalLanguage::parseFontFileName( INI *ini, void *instance, void *store, const void *userData )\n{\n\tGlobalLanguage *monkey = (GlobalLanguage *)instance;\n\tRva00439370LocalFontEntry entry;\n\tentry.m_at00 = ini->getNextAsciiString();\n\tmonkey->m_localFonts.push_front( entry );\n}\n\n// Identity is settled, the codegen is not. The GlobalLanguage FieldParse table\n// at 0x010F3AD8 pairs the string LocalFontFile at row 0x010F3B18 with this\n// body, and INI::parseLanguageDefinition at 0x00438F70 hands that table to\n// initFromINI. Zero Hour's GlobalLanguage.cpp registers its own LocalFontFile\n// row against GlobalLanguage::parseFontFileName, whose three lines are the\n// three this body runs.\n//\n// The list element is not a bare AsciiString. Retail allocates a 16-byte node\n// and copy-constructs the payload at node+8 through 0x00439370, an 81-byte\n// body that copies two StringBase<char> members at +0 and +4. The parser\n// zeroes both of them, assigns only the first through StringBase<char>::set,\n// and destroys both at the end. So the element is a two-AsciiString record and\n// 0x00439370 is its copy constructor, currently parked in\n// Code/gen_small/tgrid_115.cpp under a gen-tgrid pair name. That row is a\n// scaffold, so add_match --replace-rva can take it over with the record.\n//\n// What is left: 151 bytes against retail's 203, 91 non-relocation bytes\n// differing. Retail's frame is four dwords and holds the node payload pointer\n// in a local at esp-0x18 as well as in the dead instance argument slot. Every\n// spelling tried keeps it in a register. Retail also sinks the two zero stores\n// for the record past the getNextAsciiString call while ours runs them first.\n// STLport's own list<T>::push_front was tried first and calls _M_create_node\n// out of line, which is 174 bytes and further away than this hand-written\n// insert.\n"
}