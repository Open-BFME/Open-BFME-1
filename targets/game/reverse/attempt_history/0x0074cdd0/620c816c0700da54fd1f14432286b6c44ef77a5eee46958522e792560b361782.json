{
  "schema": 1,
  "sha256": "620c816c0700da54fd1f14432286b6c44ef77a5eee46958522e792560b361782",
  "symbol": "?query@Rva0074CDD0@@QAEEHH@Z",
  "score_kind": "author-estimate",
  "score": 0.27,
  "source": "// ?query@Rva0074CDD0@@QAEEHH@Z\n// partial score=0.27 date=2026-09-21\n// cl: /DNDEBUG /MD /EHsc\n//\n// ?query@Rva0074CDD0@@QAEEHH@Z -- 271B, thiscall(int x, int y) -> unsigned\n// char. Full boundary decoded via dis_retail.py: a cliff-mapped-texture\n// guard (index = (originY+y)*width + originX+x, bounds-checked against\n// count, then cliffInfo[index] != 0) gated on a pinned callee at 0x0074BD10\n// (thunk 0x000354EA); on a hit it returns (unsigned char)y. Any guard\n// failure -- including the pinned callee returning false -- falls straight\n// into a second, height-interpolation block using a DIFFERENT (unadjusted)\n// index = width*y + x, four neighbouring heights.x87 range test against a\n// threshold, and an integer diagonal-difference compare. Field offsets are\n// read directly off the disassembly, not guessed: width @ +0x08, count\n// @ +0x20, heights @ +0x24, cliffInfo @ +0x94, originX @ +0x120e0,\n// originY @ +0x120e4. Owning class is not identified; address-derived name\n// only, per docs/naming_evidence.md.\n\n#include <stdlib.h>\n\nextern float rva012BB2D4Scale, rva012BB2D0Threshold;\n\nclass Rva0074CDD0\n{\npublic:\n\tunsigned char query(int x, int y);\n\tbool rva0074BD10(int x, int y);\n\nprivate:\n\tunsigned char pad1[8];\n\tint width;                    // +0x08\n\tunsigned char pad2[0x14];\n\tint count;                    // +0x20\n\tunsigned short *heights;      // +0x24\n\tunsigned char pad3[0x94 - 0x28];\n\tvoid **cliffInfo;             // +0x94\n\tunsigned char pad4[0x120e0 - 0x98];\n\tint originX;                  // +0x120e0\n\tint originY;                  // +0x120e4\n};\n\nunsigned char Rva0074CDD0::query(int x, int y)\n{\n\tint w = width;\n\tint index = (originY + y) * w + originX + x;\n\tif (index >= 0 && index < count && cliffInfo[index] != 0 && rva0074BD10(x, y))\n\t\treturn static_cast<unsigned char>(y);\n\n\tint idx2 = w * y + x;\n\tif (idx2 >= 0 && idx2 < count && heights && idx2 < count - w - 1)\n\t{\n\t\tunsigned short *p12 = heights + idx2;\n\t\tint h1 = p12[0];\n\t\tint h2 = p12[1];\n\t\tunsigned short *p34 = p12 + w;\n\t\tint h3 = p34[0];\n\t\tint h4 = p34[1];\n\n\t\tint hi = h1, lo = h1;\n\t\tif (lo > h2) lo = h2;\n\t\tif (hi < h2) hi = h2;\n\t\tif (lo > h4) lo = h4;\n\t\tif (hi < h4) hi = h4;\n\t\tif (lo > h3) lo = h3;\n\t\tif (hi < h3) hi = h3;\n\n\t\tif ((hi - lo) * rva012BB2D4Scale > rva012BB2D0Threshold)\n\t\t{\n\t\t\tint firstDiagonal = abs(h2 - h3);\n\t\t\tint secondDiagonal = abs(h1 - h4);\n\t\t\treturn secondDiagonal > firstDiagonal;\n\t\t}\n\t}\n\n\treturn 0;\n}\n"
}