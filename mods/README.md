# mods/: features patched into retail `lotrbfme.exe`

See **`docs/mods.md`** — the rules, the cave mechanism, and the gotchas that
have already shipped into a binary.

`tools/modbuild.py` is the authority on which features exist; nothing here
lists them, deliberately.
